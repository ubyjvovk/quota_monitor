---
id: T-0004
title: Make ChatGPT quota reliable by retiring the unreachable live endpoint
priority: P0
complexity: C2
depends_on: []
scope: [QuotaKit/Sources/QuotaKit/Providers/CodexSources.swift, QuotaKit/Sources/QuotaKit/Providers/ProviderCatalog.swift, QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift]
---

## Goal
Every `quotactl` run currently ends the ChatGPT block with:

```
Cached — live refresh failed: ChatGPT usage endpoint returned HTTP 404
```

That line is noise, and it is misleading: it implies a transient outage the user
could wait out. The PM established by hand that it is permanent.

**Findings (PM, 2026-08-29 — do not re-investigate, and do not try to work
around any of it):**
- `https://chatgpt.com/backend-api/api/codex/usage` is the *correct* path — it
  appears verbatim in the Codex CLI binary.
- The host sits behind Cloudflare bot protection. Any plain HTTP client gets
  **403 with an HTML challenge page**; the 404 we surface is that wall, not a
  missing route.
- Codex itself never calls a usage endpoint. It learns limits from **response
  headers** (`x-codex-active-limit`) on real API turns and writes the resulting
  `rate_limits` payload into its session rollout files.
- Therefore `CodexLocalSource` — which reads those rollouts — is not a fallback.
  **It is the only real source of ChatGPT quota**, and it is accurate as of the
  user's last Codex turn.

Make the code say what is true: the local rollout is the source, and a reading
older than its window is a fact about Codex usage, not an error.

## Context
- `CodexLiveSource` is at the bottom of `Providers/CodexSources.swift`. It
  already reads its endpoint from `QUOTA_MONITOR_CODEX_USAGE_URL` with a
  hard-coded default.
- `ProviderCatalog.all` (in `Providers/ProviderCatalog.swift`) builds the
  `HybridProvider` for `codex` and currently passes a live source
  unconditionally.
- `HybridProvider` already handles "local only": pass `live: nil`, or
  `liveEnabled: false`. Read its `fetch()` before changing anything — the
  status-message logic you need is already there.
- A rollout's real payload shape, for reference:
  `{"rate_limits":{"limit_id":"codex","primary":{"used_percent":14.0,
  "window_minutes":300,"resets_at":1788038896},"secondary":{"used_percent":18.0,
  "window_minutes":10080,"resets_at":1788511023},
  "credits":{"has_credits":false,"unlimited":false,"balance":"0"}}}`

## Design — implement exactly this
1. **The live source becomes opt-in.** `CodexLiveSource` stays in the file
   (unchanged in behaviour) but is only wired up when the environment sets
   `QUOTA_MONITOR_CODEX_USAGE_URL`. Add to `Codex`:

   ```swift
   /// The ChatGPT backend sits behind bot protection and refuses plain HTTP
   /// clients, so no live source is wired up unless the user points us at a
   /// reachable endpoint themselves. Quota comes from the session rollouts.
   public static var liveSourceIfConfigured: (any QuotaSource)?
   ```
   It returns a `CodexLiveSource` when `QUOTA_MONITOR_CODEX_USAGE_URL` is set
   and parses as a URL, and `nil` otherwise. `ProviderCatalog.all` passes that
   as the codex provider's `live`.

2. **Say something true when the reading is stale.** `CodexLocalSource` keeps
   returning the newest rollout reading. No change to its parsing. The status
   wording is the fix, and it belongs where `HybridProvider` already sets
   `.needsSetup` for a fully-reset snapshot — but **do not edit
   `QuotaSource.swift`** (out of scope). Instead, `CodexLocalSource.fetch()`
   sets the returned snapshot's `status` itself when every window has reset:

   ```
   ChatGPT reports usage only after a Codex turn — last reading <age> ago
   ```
   using `QuotaFormat.age`. When at least one window still has a live reading,
   leave `status` nil.

3. **Nothing else changes.** Do not delete `CodexLiveSource`, do not touch the
   Claude sources, do not alter the rollout parser.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] A test asserts `Codex.liveSourceIfConfigured` is `nil` when
      `QUOTA_MONITOR_CODEX_USAGE_URL` is unset, and non-nil when it is set
      (set/unset it inside the test via `setenv`/`unsetenv` and restore it)
- [ ] A test asserts `ProviderCatalog.all()` yields a codex provider whose
      `live` is `nil` in a clean environment — the regression that would bring
      the 404 line back
- [ ] A test builds a rollout whose windows have all reset and asserts the
      resulting status contains `only after a Codex turn` and **does not**
      contain `404` or `failed`
- [ ] A test asserts a *fresh* rollout reading yields `status == nil`
- [ ] Existing codex tests (`codexLocalSourceReadsTheNewestRateLimitRecord`,
      `codexSnapshotRanksTheMostConstrainedWindowFirst`,
      `codexToleratesRecordsWithoutCredits`) still pass unmodified — confirm in
      the report
- [ ] `swift run --disable-sandbox --package-path QuotaKit quotactl` output
      pasted into the Worker report, showing the ChatGPT block with no
      `live refresh failed` line
- [ ] Every new public symbol has a doc comment
- [ ] No files changed outside scope

## Out of scope
- **Trying to reach the ChatGPT backend anyway.** Do not spoof a browser
  User-Agent, do not add cookies, do not attempt to bypass the bot protection.
  This is a deliberate product decision, and the existing comment in
  `ClaudeSources.swift` ("Identifies honestly… nothing to gain by pretending")
  is the standing policy.
- Shelling out to the `codex` binary, or starting `codex app-server`.
- `ClaudeSources.swift`, `Keychain.swift`, `main.swift`, `ConsoleReport.swift`.
- Credits / extra-usage rendering — T-0005 owns that.
- Anything under `App/`, `Widget/`, `Sources/QuotaKit/UI/`.

## Worker report

> claimed_by: codex (pid 5605) at 2026-08-29T17:20:46Z

### Files changed

- `QuotaKit/Sources/QuotaKit/Providers/CodexSources.swift` — added the opt-in live-source factory and the Codex-turn explanation for fully reset local rollouts.
- `QuotaKit/Sources/QuotaKit/Providers/ProviderCatalog.swift` — wires the Codex live source only when explicitly configured.
- `QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift` — covers clean/configured environments and stale/fresh rollout statuses.

### Commands and checks

- Baseline: `bash .tigerteam/scripts/run-tests.sh`

  ```text
  cmd:  swift test --disable-sandbox --package-path QuotaKit
  exit: 0
  log:  .tigerteam/logs/tests/20260829-172116-18296.log (148 lines — grep it for specific failures; do not read it whole)
  ✔ Test run with 50 tests in 0 suites passed after 0.007 seconds.
  ```

- Final: `bash .tigerteam/scripts/run-tests.sh`

  ```text
  cmd:  swift test --disable-sandbox --package-path QuotaKit
  exit: 0
  log:  .tigerteam/logs/tests/20260829-172306-18726.log (136 lines — grep it for specific failures; do not read it whole)
  ✔ Test run with 54 tests in 1 suite passed after 0.007 seconds.
  ```

- Confirmed the three existing Codex tests remain unmodified and pass:

  ```text
  ✔ Test codexToleratesRecordsWithoutCredits() passed after 0.004 seconds.
  ✔ Test codexLocalSourceReadsTheNewestRateLimitRecord() passed after 0.006 seconds.
  ✔ Test codexSnapshotRanksTheMostConstrainedWindowFirst() passed after 0.006 seconds.
  ```

- `env -u QUOTA_MONITOR_CODEX_USAGE_URL swift run --disable-sandbox --package-path QuotaKit quotactl` exited 0. Quota output:

  ```text
  Claude          max                                 live · just now
    Fable wk        20.0%  resets in 1d 20h
    Week            14.0%  resets in 1d 20h
    5h              12.0%  resets in 1h 36m
    credits         20.00 remaining

  ChatGPT         plus                                cached · 13s ago
    Week            18.0%  resets in 5d 15h
    5h              16.0%  resets in 4h 4m
    credits         0 remaining
  ```

  The ChatGPT block contains no `live refresh failed` line. Under Seatbelt, Foundation also emitted cache-database authorization warnings for `~/Library/Caches/quotactl`; they did not affect the command result and are outside this ticket's scope.

- `git diff --check` exited 0. Pre-commit `git status --short` listed exactly the three scoped files.

### Documentation

- Added the required doc comment to the new public `Codex.liveSourceIfConfigured` property. No documentation files were named by the ticket.

### Commit

- `a22c625` on `tigerteam/T-0004` — `[T-0004] Make ChatGPT quota reliable by retiring the unreachable live endpoint`

### Reviewer notes

- Environment-mutating tests restore the original value and are serialized to avoid racing each other.
- No out-of-scope files were changed; `CodexLiveSource` behaviour and rollout parsing were left intact.

> handoff: review
