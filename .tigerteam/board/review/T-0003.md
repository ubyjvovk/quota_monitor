---
id: T-0003
title: Parse the real Claude usage payload, including the scoped weekly limit it currently misses
priority: P1
complexity: C2
depends_on: []
scope: [QuotaKit/Sources/QuotaKit/Providers/ClaudeSources.swift, QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift, QuotaKit/Tests/QuotaKitTests/Fixtures/claude-usage-live.json]
---

## Goal
`Claude.windows(from:)` looks for three hard-coded top-level keys —
`five_hour`, `seven_day`, `seven_day_opus`. The PM captured a real response
from `GET https://api.anthropic.com/api/oauth/usage` (saved verbatim as the
fixture in `scope`) and that shape has moved on:

- `seven_day_opus` is `null`; so are `seven_day_sonnet`, `seven_day_cowork`
  and a dozen other buckets. The hard-coded "Opus wk" window is dead.
- The response carries a canonical, **self-describing `limits` array** —
  each entry has `kind`, `group`, `percent`, `severity`, `resets_at`,
  `is_active`, and an optional `scope.model.display_name`.
- That array contains a `weekly_scoped` entry at **20%** scoped to a model,
  `is_active: true` — the most constrained limit on the account. The current
  code reports 14% as the worst weekly figure and **never shows the 20% at
  all**. That is the bug: the tool under-reports how close the user is to
  their limit, which is the one thing it exists to get right.

Parse `limits` as the primary source, keeping the top-level keys as a
fallback for responses that lack it.

## Context
- Fixture (already committed, real shape, no credentials in it):
  `QuotaKit/Tests/QuotaKitTests/Fixtures/claude-usage-live.json`.
  Load it with `Bundle.module` like the existing fixture helpers do.
- **The response has no `rate_limits` wrapper** — windows sit at the top
  level. `ClaudeLiveSource.fetch` already handles this via
  `root.firstValue(forKey: "rate_limits") ?? root`; leave that alone.
- The percentage key here is `utilization` (already in the `forAnyKey` list);
  in the `limits` array it is `percent`.
- `ClaudeLocalSource` reads the statusline mirror, which uses the older
  `rate_limits` + `used_percentage` shape. **It must keep working unchanged** —
  its fixture is `claude-mirror.json` and the test
  `claudeMirrorParsesBothWindows` must still pass untouched.
- `QuotaWindow` is `{id, label, kind (.session/.weekly), usedPercent, resetsAt,
  windowMinutes}`. `ProviderSnapshot.credits` exists and is currently only
  populated by the Codex source.

## Design — implement exactly this
Add to `enum Claude`:

```swift
/// Builds windows from the `limits` array — the endpoint's canonical,
/// self-describing list. Returns nil when the document has no usable array,
/// so the caller can fall back to the legacy top-level keys.
static func windowsFromLimits(_ root: JSONValue) -> [QuotaWindow]?
```

Mapping, per entry in `limits`:
- `id` — the entry's `kind` (`session`, `weekly_all`, `weekly_scoped`).
- `label` — `"5h"` for `group == "session"`; `"Week"` for `weekly_all`;
  for `weekly_scoped`, the model display name when present
  (`scope.model.display_name` → `"Fable wk"`), else `"Week (scoped)"`.
- `kind` — `.session` when `group == "session"`, else `.weekly`.
- `windowMinutes` — `300` for session, `60 * 24 * 7` for weekly.
- `usedPercent` — `percent`; `resetsAt` — `resets_at`.
- **Skip** entries whose `percent` is missing or null.
- Preserve the order the array gives.

`Claude.windows(from:)` becomes: `windowsFromLimits(root) ?? <existing
per-key logic>`. Return the legacy path whenever `limits` is absent, not an
array, or yields no usable entries — so the mirror keeps working.

Also map `spend` → `ProviderSnapshot.credits` in `Claude.snapshot(...)`:
- `balance` — remaining credits as a display string derived from
  `limit.amount_minor - used.amount_minor` scaled by `exponent`
  (`2000` minor with exponent `2` → `"20.00"`), `nil` if either side is absent.
- `unlimited` — `false`.
- When `spend` is absent, `credits` stays `nil`.

**Do not surface the codenamed buckets** (`tangelo`, `iguana_necktie`,
`nimbus_quill`, `omelette_promotional`, `cinder_cove`, `amber_ladder`,
`juniper_tide`). They are unreleased server-side experiments, several report
`0.0` utilization, and showing them as real quota would be wrong. The
`limits`-array path excludes them for free; make sure the fallback path does
too by keeping its explicit `knownWindows` allowlist.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] Existing test `claudeMirrorParsesBothWindows` still passes **unmodified** —
      confirm this explicitly in the Worker report
- [ ] Existing test `claudeParsingAcceptsAlternateKeySpellings` still passes
- [ ] New tests driven by `claude-usage-live.json` asserting:
      - exactly three windows are produced, in array order
      - the `weekly_scoped` window is present with `usedPercent == 20` and
        label `"Fable wk"` — the regression this ticket exists to prevent
      - the session window is `usedPercent == 10`, kind `.session`
      - **no** window is produced for `nimbus_quill`, `tangelo`, or any other
        codenamed bucket, and none for the `null` entries
      - `credits?.balance == "20.00"`
- [ ] A test asserts the fallback still fires: a document with no `limits` key
      but with top-level `five_hour`/`seven_day` yields those two windows
- [ ] Every new function has a doc comment explaining *why* `limits` is
      preferred over the top-level keys
- [ ] No files changed outside scope

## Out of scope
- `Keychain.swift` and how the token is obtained (T-0001 owns it). You cannot
  reach the real endpoint from the worker sandbox and must not try — the
  fixture is the input.
- `main.swift` / console rendering (T-0002 owns it).
- `CodexSources.swift`.
- Acting on `severity` or `is_active`, or adding a notion of "active window" to
  the model. Worth doing later; not here.

## Worker report

> claimed_by: codex (pid 94579) at 2026-08-29T16:34:04Z
> RETRY: killed by operator at 2026-08-29T16:37:49Z (no attempt penalty)

> claimed_by: codex (pid 94579) at 2026-08-29T16:37:49Z
> RETRY: killed by operator at 2026-08-29T16:38:21Z (no attempt penalty)

> claimed_by: codex (pid 5605) at 2026-08-29T16:41:19Z

### Files changed
- `QuotaKit/Sources/QuotaKit/Providers/ClaudeSources.swift` — prefer canonical
  `limits` entries, preserve legacy-key fallback, and map `spend` to credits.
- `QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift` — cover the real live
  fixture, scoped weekly window, exclusions, spend balance, and legacy fallback.
- `QuotaKit/Tests/QuotaKitTests/Fixtures/claude-usage-live.json` — unchanged;
  the already-committed real response fixture drives the new regression test.

### Verification
- `bash .tigerteam/scripts/run-tests.sh` — exit 0.
  - `cmd:  swift test --disable-sandbox --package-path QuotaKit`
  - `exit: 0`
  - `log:  .tigerteam/logs/tests/20260829-164150-6825.log (94 lines — grep it for specific failures; do not read it whole)`
  - `Test run with 39 tests in 0 suites passed after 0.005 seconds.`
- The existing, unmodified `claudeMirrorParsesBothWindows` test passed after
  0.003 seconds.
- The existing `claudeParsingAcceptsAlternateKeySpellings` test passed after
  0.001 seconds.
- `claudeLiveUsageParsesCanonicalLimitsAndSpend` and
  `claudeFallsBackToLegacyWindowsWhenLimitsAreAbsent` both passed.
- `git diff --check -- QuotaKit/Sources/QuotaKit/Providers/ClaudeSources.swift QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift` — exit 0.

### Documentation
- Added a doc comment to `windowsFromLimits(_:)` explaining that the canonical
  array is preferred because legacy top-level keys omit scoped limits.
- Updated the existing `windows(from:)` doc comment to explain the legacy
  statusLine/old-response fallback.

### Commit
- `a6108b17cb3de46fc1036342229603c9016ff0a8` on `tigerteam/T-0003`.

### Review notes
- Only the two scoped source/test files were modified; no out-of-scope issues
  were encountered.

> handoff: review
