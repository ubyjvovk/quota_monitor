---
id: T-0068
title: Doc sync — README describes CalVer, the version surfaces, and the renamed plugin id
priority: P2
complexity: C1
depends_on: [T-0066, T-0067]
scope: [README.md]
---

## Goal
T-0064 through T-0067 changed how this project is versioned and what each
component reports. `README.md` still documents the old scheme and the old
Omarchy install path. Bring it in line with what shipped — no new behaviour,
no new files, just the documentation pass the other four tickets deliberately
left out so they would not collide on one file.

## Context
Read the four tickets in `.tigerteam/board/done/` (T-0064, T-0065, T-0066,
T-0067) and the code they landed; document what is actually there, not what
this ticket says should be there. If any statement below contradicts the
landed code, the code wins — say so in the Worker report rather than writing
something false.

What landed:
- `VERSION` at the repo root is the single source of truth. The scheme is
  CalVer `YYYY.M.MICRO`: 4-digit year, unpadded month, and the release count
  within that month starting at 1 (`2026.9.1`, `2026.9.2`, `2026.10.1`).
  MICRO is not the day of the month. Current value: `2026.9.1`.
- `scripts/set-version.sh <calver>` is the only supported way to bump it; it
  writes `VERSION`, `project.yml`'s `MARKETING_VERSION`, and
  `omarchy/manifest.json`'s `version` together.
- `scripts/check-versions.sh` fails the test suite when those drift; it runs
  as part of `scripts/test-all.sh`.
- `scripts/release.sh` now tags `v$(cat VERSION)` instead of computing
  `{major}.{commit count}`, and `--next` prints the suggested next CalVer.
- The Go core is stamped at link time from `VERSION`; `quotamon --version`
  prints `quotamon 2026.9.1`, and the snapshot JSON carries a top-level
  `version` key.
- The macOS About panel shows the app version plus the bundled core's version.
- The Omarchy plugin id is now `ubyjvovk.quotamon`, so it installs to
  `~/.config/omarchy/plugins/ubyjvovk.quotamon/`; its panel footer reports the
  plugin version and the quotamon version, and warns when the installed
  quotamon is older than the manifest's declared minimum.
- The repo now has an MIT `LICENSE`.

Sections of `README.md` that are stale or affected:
- **"Contributing / for agents" → the `Releases:` bullet** (~line 287) still
  describes `{major}.{commit count}` and `0.264`.
- **"Omarchy / Waybar" → "Developing the plugin"** (~line 137) uses
  `~/.config/omarchy/plugins/quotamon` in three commands, including
  `omarchy plugin enable quotamon --section center --after omarchy.clock`.
- Wherever the snapshot JSON's shape is described, the new `version` key
  belongs there.
- Line ~27 links to `core/README.md`, **which does not exist**. Fix the link
  (point at `core/` or drop the link) — do not create that file.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (nothing should break; run
      it to prove the doc-only change is doc-only)
- [ ] The `Releases:` bullet documents CalVer `YYYY.M.MICRO`, the `VERSION`
      file, `scripts/set-version.sh`, `scripts/check-versions.sh`,
      `scripts/release.sh --next`, and that the tag is `v<VERSION>`; no text
      claiming a commit-count version survives anywhere
      (`grep -in "commit count" README.md` returns nothing)
- [ ] `grep -n "plugins/quotamon\b" README.md` returns nothing, and
      `grep -n "plugin enable quotamon" README.md` returns nothing — every
      Omarchy path and command uses `ubyjvovk.quotamon`
- [ ] README states, in the appropriate existing sections, that
      `quotamon --version` reports the core version, that the macOS About panel
      shows app + core versions, and that the Omarchy panel footer shows plugin
      + core versions
- [ ] The `core/README.md` link is corrected and no new files are created
- [ ] A licence line is added (MIT, `LICENSE`) — one line, in the
      "Contributing / for agents" section
- [ ] Every command or path written in the README is one that exists in the
      tree as landed; state in the Worker report which ones you actually ran
- [ ] No files changed outside scope — `README.md` is the only file this
      ticket may touch

## Out of scope
- `AGENTS.md`, `PROVIDERS.md`, `GO-PORT.md`, `omarchy/README.md`,
  `.tigerteam/**` — other owners, already handled, or PM-owned.
- Creating `core/README.md` or any other new documentation file.
- Restructuring the README, rewriting the provider list, regenerating
  screenshots, or editing anything the version work did not change.
- Any code change whatsoever.

## Worker report
