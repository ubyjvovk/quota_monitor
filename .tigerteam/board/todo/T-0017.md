---
id: T-0017
title: Hybrid merge must keep a provider that reports credits/spend but no windows
priority: P0
complexity: C1
depends_on: []
scope: [core/internal/hybrid/, core/cmd/quotamon/waybar.go, core/cmd/quotamon/waybar_test.go]
---

## Goal
`hybrid.Provider.Fetch` treats a successful source result as usable only when
`len(Windows) > 0`. DeepInfra (T-0013) is pay-as-you-go: with no spending
limit it has **zero windows by design** and carries its reading in
`Credits.Balance` ("$7.75 this month"). Today that result is discarded and the
provider is reported as `needsSetup: No data source configured` — wrong, and
the PM's spec error, not T-0013's.

## Context
- `core/internal/hybrid/provider.go` (the merge) and `core/cmd/quotamon/waybar.go`
  (tooltip). The table renderer is NOT part of this ticket (T-0020 handles it).
- Credits rules come from Swift `ConsoleReport.creditsLine` (QuotaKit/Sources/QuotaKit/Support/ConsoleReport.swift).

## Design — implement exactly this
- In `core/internal/hybrid/provider.go` define
  `func usable(p snapshot.Provider) bool { return len(p.Windows) > 0 || p.Credits != nil }`
  and use it in both the live-wins and local-fallback checks instead of the
  window-count test. The "every window rolled over" status logic applies only
  when `len(Windows) > 0`.
- In `core/cmd/quotamon/waybar.go` `providerTooltip`: when a provider has no
  windows but has credits, print one line `  <balance>` (or `unlimited`) instead
  of nothing, so DeepInfra shows its spend in the tooltip. Text stays `DI —`.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] hybrid test: live source returning `Provider{Windows: nil, Credits: &Credits{Unlimited:true, Balance:"$7.75 this month", Enabled:true}}` wins and is returned unchanged with status ok; the same from local (live failing) is returned with the "Cached — live refresh failed" status; a result with neither windows nor credits is still treated as nothing
- [ ] waybar test: windowless provider with credits renders tooltip line `  $7.75 this month`
- [ ] `core/internal/hybrid/provider.go` has a comment on `usable` explaining the DeepInfra case
- [ ] No files changed outside scope

## Out of scope
The table renderer (`table.go` — T-0020), providers, registry.

## Worker report

> claimed_by: codex-1 (pid 65930) at 2026-08-29T20:29:55Z

- Status: safe hybrid/Waybar partial committed; blocked on the contradictory,
  unavailable table-renderer work described below.
- Files changed:
  - `core/internal/hybrid/provider.go` — accept credit-bearing snapshots and
    limit rollover handling to snapshots that have windows.
  - `core/internal/hybrid/provider_test.go` — cover live credits, cached local
    credits after a live failure, and empty successful snapshots.
  - `core/cmd/quotamon/waybar.go` — show a balance (preferred when present) or
    `unlimited` for a windowless provider's credits.
  - `core/cmd/quotamon/waybar_test.go` — cover the DeepInfra balance and
    unlimited tooltip lines while preserving `DI —` text.
- Commands run:
  - `bash .tigerteam/scripts/run-tests.sh` (baseline) — exit 0,
    `.tigerteam/logs/tests/20260829-203020-75151.log`; summary:
    `✔ Test run with 62 tests in 1 suite passed`; Go packages including
    `quotamon/cmd/quotamon` and `quotamon/internal/hybrid` reported `ok`;
    `### exit 0`.
  - `bash .tigerteam/scripts/run-tests.sh` (first implementation) — exit 1,
    `.tigerteam/logs/tests/20260829-203210-75964.log`; summary: Swift's
    62 tests passed, then Go vet reported that `Credits.Balance` is `*string`;
    `### exit 1`. This was corrected in the committed work.
  - `bash .tigerteam/scripts/run-tests.sh` (pointer correction) — exit 0,
    `.tigerteam/logs/tests/20260829-203239-76101.log`; summary:
    `✔ Test run with 62 tests in 1 suite passed`; all Go packages reported
    `ok`; `### exit 0`.
  - `bash .tigerteam/scripts/run-tests.sh` (final balance-precedence check) —
    exit 0, `.tigerteam/logs/tests/20260829-203322-76303.log`; summary:
    `✔ Test run with 62 tests in 1 suite passed`; `quotamon/cmd/quotamon` and
    `quotamon/internal/hybrid` reported `ok`; all other Go packages reported
    `ok` or `[no test files]`; `### exit 0`.
- Documentation: added the required `usable` comment explaining DeepInfra's
  windowless pay-as-you-go case; no public API was added.
- Commit: `df19fda0fbc6c539810907c2f705f0e82af58af6` on
  `tigerteam/T-0017`.
- Review closely: `Credits.Balance` takes precedence over `Unlimited` in the
  Waybar tooltip so DeepInfra displays `$7.75 this month` even when it has no
  spending limit.

## Questions

The ticket changed during this attempt to add `table.go` and `table_test.go`
to `scope` and to add a table-renderer acceptance criterion. That conflicts
with its still-current `Out of scope` section, which says the table renderer is
owned by T-0015 and must not be touched. Both newly named files are also absent
from this branch, so the criterion cannot be implemented or tested here.

Options:

1. Remove the table scope/criterion from T-0017 and leave that behavior with
   T-0015, after which the committed hybrid/Waybar work can be reviewed.
2. Land T-0015 first, make it an explicit dependency, and remove the table
   renderer from this ticket's `Out of scope` section before resuming T-0017.

Recommendation: use option 1, preserving the ticket's existing ownership fence
and avoiding overlapping implementation responsibility.

> handoff: blocked

## Answers

PM answer: option 1 — my mid-flight edit was the mistake. The ticket is
restored to hybrid + waybar only (scope, criteria and Out-of-scope now agree);
the table rule moves to T-0020. Your commit df19fda stands — run the tests
once more and hand off to review.

