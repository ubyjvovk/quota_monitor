---
id: T-0069
title: release.sh resolves the repo root so it works from any directory
priority: P2
complexity: C1
depends_on: []
scope: [scripts/release.sh]
---

## Goal
`scripts/release.sh` reads the version with a bare `cat VERSION`, so it only
works when the caller's current directory happens to be the repo root. Run from
anywhere else it dies with `cat: VERSION: No such file or directory` and exit 1
— including `./scripts/release.sh --dry-run`, which is documented as safe to run
from anywhere. Before T-0065 the script derived its tag from `git rev-list`,
which works from any directory inside a checkout, so this is a regression in the
release path. Fix it the way `scripts/build.sh` already does.

## Context
- `scripts/build.sh` (lines 11-13) is the pattern to copy verbatim:
  ```bash
  ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
  cd "$ROOT"
  ```
- `scripts/release.sh` currently has no `ROOT` resolution and no `cd`. The
  offending line is `TAG="v$(cat VERSION)"`.
- The `--next` branch runs `git tag --list` before that line; git commands work
  from any directory in a checkout, so `--next` is not currently broken — but it
  must keep working after the change.
- The script's other guards (master-only, clean tree, tag-does-not-exist) are
  all git-based and unaffected.
- Reproduce the bug: `cd core && bash ../scripts/release.sh --dry-run`.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] `cd core && bash ../scripts/release.sh --dry-run` prints `v2026.9.1` and
      exits 0 (this is the bug; record the before/after in the Worker report)
- [ ] `cd core && bash ../scripts/release.sh --next` prints a valid CalVer and
      exits 0
- [ ] From the repo root, `bash scripts/release.sh --dry-run` still prints
      `v2026.9.1` and `bash scripts/release.sh --next` still prints a valid
      CalVer
- [ ] `bash -n scripts/release.sh` exits 0
- [ ] The header comment still describes CalVer and the three subcommands; add
      nothing else to it
- [ ] No files changed outside scope

## Out of scope
- Every other script, including `scripts/build.sh` (already correct),
  `scripts/set-version.sh` and `scripts/check-versions.sh` (both already
  resolve `ROOT` themselves).
- Changing what `--next` computes, the tag format, or any of the three guards.
- Actually cutting a release, creating a tag, or running `git push` — never
  push, and never create a tag even locally.
- Refactoring the script beyond the root resolution.

## Worker report
