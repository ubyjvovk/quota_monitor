---
id: T-0053
title: Swift: history never records rolled-over windows as 0%; short names match the Go catalog
priority: P2
complexity: C2
depends_on: []
scope: [QuotaKit/Sources/QuotaKit/Engine/UsageHistory.swift, QuotaKit/Sources/QuotaKit/Models/ProviderSnapshot.swift, QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift]
---

## Goal
Two consolidated defects (opus F6+codex F15; opus F13+ds F5 — see the review
reports).

## Design — implement exactly this
- `UsageHistory.record`: sample `currentUsedPercent(asOf:)`; when nil (window
  rolled over) SKIP the sample — a gap is the honest series. Keep the
  duplicate-suppression behaviour for real readings.
- `ProviderSnapshot.shortNames`: extend to the full catalog matching
  `core/cmd/quotamon/waybar.go` exactly — grok→GK, deepinfra→DI, kimi→KM
  (claude→CL, codex→GPT unchanged); prefix fallback stays for unknown ids.
  Add a comment naming waybar.go as the source the map must track.
- Tests (swift-testing): rolled-over window produces no new sample; the five
  short names asserted against the Go values.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] The two tests above exist and pass
- [ ] No files changed outside scope

## Out of scope
Sparkline consumers, the widget (T-0054), waybar.go itself.

## Worker report
