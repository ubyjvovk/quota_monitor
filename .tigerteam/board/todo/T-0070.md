---
id: T-0070
title: Omarchy plugin releases — publish tags a CalVer, and the plugin repo cuts its own GitHub Release
priority: P1
complexity: C2
depends_on: []
scope: [scripts/publish-omarchy-plugin.sh, omarchy/.github/workflows/release.yml, omarchy/README.md, README.md]
---

## Goal
The standalone plugin repository `ubyjvovk/quotamon-omarchy` has **no tags and
no releases**, and its published `manifest.json` still reads
`"id": "quotamon"`, `"version": "1.0.0"` — the pre-CalVer state. Its `master`
is the subtree split of T-0061 (2026-08-31), so everything the CalVer wave
landed under `omarchy/` (T-0067's namespaced id, licence, dependencies block
and About footer; T-0064..T-0069's version plumbing) is unpublished.

The immediate cause is that `scripts/publish-omarchy-plugin.sh` was simply not
re-run. The structural cause is that it is a bare `git subtree split` +
force-push: it pushes no tag, cuts no release, and does not check that what it
is about to publish carries the current `VERSION`. Nothing in the release path
reminds anyone it exists, so a published plugin can silently sit months behind
the manifest version it claims.

Close the structural gap. **Do not publish anything** — see Out of scope.

## Context
- Current state, confirmed against the GitHub API on 2026-09-01:
  - `quotamon-omarchy`: 0 tags, 0 releases, `master` at `1c076f6`
    (`[T-0061] Omarchy panel opens anchored to its bar icon`), pushed
    2026-08-31T19:42Z; published `manifest.json` version `1.0.0`, id `quotamon`.
  - `quota_monitor`: release `v2026.9.1` exists and is healthy.
- `scripts/publish-omarchy-plugin.sh` today: refuses off-master, refuses a dirty
  tracked tree, runs `git subtree split --prefix=omarchy HEAD`, prints the sha
  and target, honours `--dry-run`, then force-pushes
  `"$split_sha:refs/heads/master"`. Keep every one of those behaviours.
- `scripts/check-versions.sh` already validates that `VERSION`, `project.yml`
  and `omarchy/manifest.json` agree; it reads files and never writes. It
  resolves its own `ROOT`, so it is safe to call from the publish script.
- `scripts/release.sh` is the pattern for a tag name: `TAG="v$(cat "$ROOT/VERSION")"`.
- `.github/workflows/release.yml` (main repo) is the pattern for a tag-triggered
  release: `on: push: tags: ["v*"]`, `permissions: contents: write`, a "Verify
  tagged version" step comparing `${GITHUB_REF_NAME#v}` against the file, and
  `softprops/action-gh-release@v2` with `generate_release_notes: true`.
- `git subtree split --prefix=omarchy` carries **every** file under `omarchy/`,
  dotted directories included. So a file committed here at
  `omarchy/.github/workflows/release.yml` lands in the plugin repo at
  `.github/workflows/release.yml` and runs there. GitHub only reads workflows
  from the *repository root* `.github/workflows/`, so the same file is inert in
  `quota_monitor` — it will not run here, and that is intended, not a bug.
- The plugin repo is public and Actions are enabled by default; the workflow's
  own `GITHUB_TOKEN` with `contents: write` is enough to cut a release. No
  secret, no `gh` CLI (`gh` is not installed on this host — do not use it).

## Design (decided; implement exactly this)

**1. `scripts/publish-omarchy-plugin.sh`**

- Resolve `ROOT` the way `scripts/build.sh` does and `cd "$ROOT"`, so the script
  works from any directory inside the checkout.
- After the existing branch and clean-tree guards, and before the split, run
  `bash "$ROOT/scripts/check-versions.sh"`. On failure print
  `Refusing to publish: version drift (run scripts/check-versions.sh)` to stderr
  and exit 1. Publishing a manifest that disagrees with `VERSION` is exactly the
  bug this ticket exists to prevent.
- Compute `tag="v$(cat "$ROOT/VERSION")"`.
- Decide whether the tag already exists on the target:
  ```bash
  tag_state=unknown
  if remote_tags=$(GIT_TERMINAL_PROMPT=0 git ls-remote --tags "$target" "refs/tags/$tag" 2>/dev/null); then
    if [[ -n $remote_tags ]]; then tag_state=present; else tag_state=absent; fi
  fi
  ```
  `unknown` (no network, no credentials — the sandboxed lanes have neither) must
  never fail the script: `--dry-run` still exits 0 and reports `unknown`.
- Print, before any push and in `--dry-run` too, exactly these three lines
  followed by the tag line:
  ```
  Omarchy subtree: <sha>
  Publish target: <target> (master)
  Plugin version: <tag>
  ```
  then one of:
  - `Tag <tag>: will be created` (absent)
  - `Tag <tag>: already published — master updated only; bump with scripts/set-version.sh to cut a new plugin release` (present)
  - `Tag <tag>: unknown (could not reach <target>)` (unknown)
- Push behaviour on a real run — master **always**, tag **only when absent**:
  ```bash
  git push "$target" "$split_sha:refs/heads/master" --force
  [[ $tag_state == absent ]] && git push "$target" "$split_sha:refs/tags/$tag"
  ```
  An already-published tag is never moved or deleted: a tag must keep meaning
  the bytes it meant. `unknown` pushes master only and says so.
- `--dry-run` keeps printing `Dry run: nothing pushed` as its last line and
  exits 0 without contacting the remote for anything but the tolerated
  `ls-remote` probe above.
- Keep the existing usage string and argument parsing, including the optional
  positional remote-url override; the new output must work with an overridden
  target too.
- Update the header comment to say the script also tags the plugin repo with the
  current CalVer, which is what triggers the plugin's own release workflow.

**2. `omarchy/.github/workflows/release.yml`** (new file)

- Header comment explaining: this file lives in `quota_monitor` under
  `omarchy/`, reaches the plugin repository through the subtree split, runs only
  there, and is inert in `quota_monitor`.
- `name: release`; `on: push: tags: ["v*"]` plus `workflow_dispatch`;
  `permissions: contents: write`; `runs-on: ubuntu-latest`.
- Steps:
  1. `actions/checkout@v4`.
  2. Verify the tag against the manifest: for a `refs/tags/v*` ref, compare
     `${GITHUB_REF_NAME#v}` with the `version` value in `manifest.json` and exit
     1 on disagreement, with a message naming both. Read it without `jq`
     assumptions — a `grep`/`sed` on the `"version":` line is fine and matches
     how `scripts/check-versions.sh` parses the same file.
  3. Package: `zip -r "quotamon-omarchy-$VERSION.zip"` over the plugin files,
     excluding `.git/*` and `.github/*` — a user unzipping the asset should get
     exactly what `omarchy plugin add` would install.
  4. `softprops/action-gh-release@v2` with the zip in `files:` and
     `generate_release_notes: true`, guarded to tag pushes
     (`if: github.event_name == 'push'`), mirroring the main workflow. A
     `workflow_dispatch` run uploads the zip with `actions/upload-artifact@v4`
     instead, since there is no tag to attach a release to.

**3. Docs**

- `omarchy/README.md`: a short section (or an addition to an existing one)
  stating that this repository is *generated* — the source of truth is
  `ubyjvovk/quota_monitor` under `omarchy/`, PRs belong upstream, and `master`
  is force-pushed; that each publish tags `v<CalVer>` matching the main
  repository's release of the same version; and that the tagged zip is on the
  plugin repo's Releases page. Keep the existing install instructions intact.
- `README.md`: the release bullet around line 294-303 currently ends at the main
  workflow. Extend it so the documented release sequence is
  `scripts/set-version.sh` → commit → `scripts/release.sh` →
  `scripts/publish-omarchy-plugin.sh`, and say what the last step does now
  (mirrors `omarchy/` to the plugin repo and tags it `v<VERSION>`, which cuts
  the plugin's own release). The existing mention at line ~147-148 should point
  at that bullet rather than repeat it.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] `bash -n scripts/publish-omarchy-plugin.sh` exits 0
- [ ] From the repo root, `bash scripts/publish-omarchy-plugin.sh --dry-run`
      exits 0 and prints, in order, the `Omarchy subtree:`, `Publish target:`,
      `Plugin version: v2026.9.1`, `Tag v2026.9.1: …` and
      `Dry run: nothing pushed` lines. Paste the exact output in the report.
- [ ] `cd core && bash ../scripts/publish-omarchy-plugin.sh --dry-run` produces
      the same output and exits 0 (root resolution)
- [ ] `bash scripts/publish-omarchy-plugin.sh --dry-run git@example.com:x/y.git`
      exits 0 and reports that target
- [ ] Version-drift guard, demonstrated and then reverted: temporarily edit the
      `version` value in `omarchy/manifest.json`, confirm `--dry-run` exits 1
      with the refusal message and pushes nothing, then restore the file so
      `git status --porcelain` is clean again for that path. Record both runs.
- [ ] Bad-usage paths still behave: a second positional argument and an unknown
      `-flag` each exit 2 with the usage line
- [ ] `omarchy/.github/workflows/release.yml` exists, is valid YAML
      (`python3 -c "import yaml,sys; yaml.safe_load(open('omarchy/.github/workflows/release.yml'))"`
      — if PyYAML is unavailable on the lane, say so in the report and instead
      show the file in full), and its tag-verification step, run by hand against
      `omarchy/manifest.json` with `GITHUB_REF_NAME=v2026.9.1`, passes; with
      `GITHUB_REF_NAME=v2026.9.99` it fails. Demonstrate both.
- [ ] `git subtree split --prefix=omarchy HEAD` on the ticket branch produces a
      tree containing `.github/workflows/release.yml` at its root — verify with
      `git ls-tree -r --name-only <split_sha> | grep github` and paste the line
- [ ] Both doc changes are present and accurate; the release sequence in
      `README.md` names all four scripts in order
- [ ] No files changed outside scope

## Out of scope
- **Publishing, pushing or tagging anything, anywhere.** Do not run
  `scripts/publish-omarchy-plugin.sh` without `--dry-run`, do not
  `git push`, do not create a tag even locally, do not touch the
  `quotamon-omarchy` remote. The real publish is the owner's call and the PM
  executes it after review.
- Wiring the publish script into `scripts/test-all.sh`. The suite must stay
  offline and fast; this script talks to a remote.
- Wiring the publish into `.github/workflows/release.yml` (main repo). A
  workflow there has no push credentials for a second repository; publishing
  stays a local, deliberate maintainer step.
- `scripts/release.sh`, `scripts/set-version.sh`, `scripts/check-versions.sh`,
  `scripts/build.sh`, `scripts/package.sh` — call `check-versions.sh`, change
  none of them.
- Bumping `VERSION` or any version literal. `2026.9.1` is the current, correct,
  already-released version.
- Any change to the plugin's runtime code (`Panel.qml`, `Model.js`,
  `BarWidget.qml`, `StackedQuotaIcon.qml`, `fetch-quotamon.sh`, `manifest.json`)
  beyond the temporary, reverted drift test above.
