---
id: T-0055
title: Release/build scripts: SHA256SUMS never hashes itself; build.sh fails on compile failure
priority: P2
complexity: C1
depends_on: []
scope: [.github/, scripts/build.sh, scripts/package.sh]
---

## Goal
Consolidated findings opus F15, codex F10, codex F9 (see review reports).

## Design — implement exactly this
- `scripts/package.sh` and the workflow's notarize step: write checksums to a
  temp file then `mv` over `SHA256SUMS`, with the glob excluding it
  (`shasum -a 256 $(ls | grep -v '^SHA256SUMS$')` or explicit patterns).
  package.sh also `rm -f "$OUTDIR/SHA256SUMS"` first so a reused outdir is safe.
- `scripts/build.sh`: capture xcodebuild's real status
  (`set -o pipefail` for that pipeline or `PIPESTATUS`), exit non-zero on
  compile failure, and `rm -rf` the expected .app product path before building
  so a stale bundle can never be launched as success.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (unchanged suites)
- [ ] `bash scripts/package.sh <fake .app dir> 0.0.0 <outdir>` twice into the SAME outdir → SHA256SUMS has no SHA256SUMS entry and `shasum -c` passes (run it; hdiutil works on your lane or say so)
- [ ] `ruby -ryaml -e "YAML.load_file('.github/workflows/release.yml')"` exits 0
- [ ] `grep -n "pipefail\|PIPESTATUS" scripts/build.sh` matches
- [ ] No files changed outside scope

## Out of scope
Signing/notarization logic, workflow triggers, README.

## Worker report

> claimed_by: opus-1 (pid 83690) at 2026-08-31T15:01:21Z
