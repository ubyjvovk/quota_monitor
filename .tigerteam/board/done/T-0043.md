---
id: T-0043
title: Stress review (opus) — independent full-repo review, report only
priority: P1
complexity: C3
assignee: opus
depends_on: []
scope: [.tigerteam/reviews/2026-08-31-opus.md]
---

## Goal
An independent, adversarial review of the whole repository by you alone.
Your deliverable is ONE markdown report at
`.tigerteam/reviews/2026-08-31-opus.md` — you change NO other file. Three
other workers are running the same brief in parallel; do not coordinate, do
not hedge toward consensus — the PM consolidates afterwards and disagreement
is useful signal.

## Context
- Orient with AGENTS.md, README.md, GO-PORT.md, PROVIDERS.md first.
- Sweep, in this order of effort: `core/` (Go — providers, snapshot contract,
  CLI), `QuotaKit/Sources/` (Swift — models, engine, ConsoleTable, UI),
  `omarchy/` (QML/JS plugin), `App/` + `Widget/` (SwiftUI — read-only for
  every lane), `scripts/`, `.github/workflows/release.yml`. Skip `docs/`,
  generated dirs, and `.tigerteam/` internals (board/logs are noise).
- You may run `bash .tigerteam/scripts/run-tests.sh` (do it once, first) and
  read-only git commands. No network. Do not fix anything — report only.

## What to hunt (in priority order)
1. Correctness bugs a user would hit: wrong numbers, wrong ordering, wrong
   state after errors, time/reset-boundary handling, nil/empty-payload paths.
2. Credential/secret handling: anything that could log, leak, or mis-scope a
   token or API key; command construction; file modes.
3. Contract drift between surfaces (console table vs macOS panel vs widget vs
   waybar vs omarchy) — the repo's rule is one derived layer, transcribed.
4. Concurrency/lifecycle: the Swift engine's refresh loop, QML process
   handling, cache read/write races.
5. Failure honesty: errors swallowed, staleness presented as live, "0%" where
   "no reading" is meant.
Not wanted: style, naming, formatting, hypothetical perf, TODO archaeology.

## Report format (exact section names)
```
# Stress review — opus — 2026-08-31
## Summary            (≤5 sentences, your overall verdict)
## Findings           (5–15, ranked most severe first)
### F1 — <short title>
- file: <path>:<line>
- severity: high|medium|low   confidence: certain|likely|possible
- claim: <one sentence>
- scenario: <concrete input/state → wrong outcome>
- fix: <one line>
## What I could not verify   (mandatory — name the things you had to assume)
## Test run                  (the run-tests.sh tail: suite counts + exit)
```
Every finding cites a real file:line you actually read. A finding you could
not confirm in code goes in with confidence "possible", not omitted.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (baseline, unmodified tree)
- [ ] The report exists at exactly `.tigerteam/reviews/2026-08-31-opus.md` with all five sections and 5–15 findings, each carrying file:line, severity, confidence, scenario, fix
- [ ] `git status` on your branch shows ONLY that one new file (no other changes)
- [ ] No files changed outside scope

## Out of scope
Fixing anything, editing any source/doc file, other workers' reports,
.tigerteam/board, network access.

## Worker report

> claimed_by: opus-1 (pid 83690) at 2026-08-31T14:26:01Z

### Files changed
- `.tigerteam/reviews/2026-08-31-opus.md` (new) — the review report; the only
  file in scope and the only file touched. No source, doc, or board file edited.

### Commands run
- `bash .tigerteam/scripts/run-tests.sh` — once, first, on the unmodified tree.
  Summary lines:
  ```
  ✔ Test run with 83 tests in 2 suites passed after 0.008 seconds.
  ok  	quotamon/cmd/quotamon	2.214s   ... (16 core packages, all ok;
  ?   	quotamon/internal/fixtures	[no test files])
  omarchy model tests passed
  ### exit 0
  ```
- Read-only reads of `core/`, `QuotaKit/Sources/`, `omarchy/`, `App/`,
  `Widget/`, `scripts/package.sh`, `.github/workflows/release.yml`, and the
  fixtures. `git status --porcelain` / `git log` only. No network.
- Attempted one empirical check of the F1 exit code with a scratch
  `QUOTA_MONITOR_DIR`; the sandbox refused a temp dir outside the worktree and
  I declined to write config inside it, so the scratch dir was removed and the
  finding rests on code plus the existing test assertions. This is recorded in
  the report's "What I could not verify".

### Doc changes
None — the deliverable is the report; the ticket forbids editing any other file.

### Commit
- `b92e194` on branch `tigerteam/T-0043` (isolated checkout).
- `git status --porcelain` after the commit: empty.

### For the reviewer
- 15 findings, ranked. The three I would act on first are F1 (exit 1 on a valid
  "nothing current" snapshot, discarded by both the macOS app and the omarchy
  panel), F2 (`quotamon config get --json` prints `api_key` in cleartext while
  the human branch redacts it), and F3 (the app's 15 s process budget is below
  the CLI's own 25 s + 15 s worst case, so a Kimi refresh can never complete
  inside the app).
- F1 deserves a design call rather than a patch: the exit-1 behaviour is
  deliberate and pinned by `core/cmd/quotamon/main_test.go:95` and `:118`, so
  the fix is either to change that contract or to fix both consumers.
- F8 (`$%d` on an invoice count) and F12 (Grok's hard-coded "Week") depend on
  provider semantics I could not confirm without network; both are flagged with
  the assumption stated.
- Out-of-scope issues noticed and deliberately not filed as numbered findings
  are listed at the end of "What I could not verify": the hard-coded
  "open `kimi`" refresh message in `hybrid.PreFetch`, `accessibleDescription`
  speaking "0% used" for a rolled-over window, the unreachable `failed` status
  state, and the missing watchdog on `Panel.qml`'s `refreshing` flag.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-5 | 10m 42s | 9.37M (9.37M) | 43.82k | $7.71 | engine | review |

total: 1 attempt · 10m 42s · 9.37M in (9.37M cached) / 43.82k out · $7.71 (accepted 2026-08-31T14:38:14Z)
