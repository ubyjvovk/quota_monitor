---
id: T-0025
title: Last-reading cache for stale-token providers + Kimi refresh via the CLI itself (no rotation by us)
priority: P1
complexity: C2
depends_on: []
scope: [core/internal/providers/kimi/, core/internal/cache/, core/internal/hybrid/, core/internal/registry/, core/cmd/quotamon/main.go, core/cmd/quotamon/main_test.go]
---

## Goal
User decisions (2026-08-29): quotamon must **never rotate Kimi's refresh
token itself**, and should **refresh rarely** — a stale token means the Kimi
CLI has not run, so nothing was spent and the last reading is still true
until a window resets. Implement: (1) a per-provider last-reading cache used
whenever the credential is stale-by-timestamp; (2) when even the cache is
too old, refresh by **launching the Kimi CLI itself** for a few seconds
(it rotates and writes its own file on start), then read `/usages` natively.

## Context
- `PROVIDERS.md` § Kimi — endpoint, 15-min token, the "no rotation" policy,
  and why print mode is unusable (`-p ""` refuses; `-p "/usage"` spends).
- Verified recipe (PM, this machine): the TUI needs a pty; `script(1)` gives
  one without tmux. Keystrokes must wait for startup and use `\r`:
  `( sleep 8; printf '/exit\r'; sleep 2 ) | script -q /dev/null kimi`
  → on launch the CLI POSTs `auth.kimi.com/api/oauth/token` when the token
  is expired and rewrites `~/.kimi-code/credentials/kimi-code.json`. Linux
  `script` syntax differs: `script -q -c "kimi" /dev/null`. `/exit` may not
  terminate the process — kill it after a 20 s deadline; that is fine.
- `core/internal/providers/kimi/` (T-0022), `core/internal/hybrid/provider.go`,
  `core/internal/registry/`, `config.Path()` for the cache dir.
- Tests: no real `~/.kimi-code`, no network, no real `kimi`/`script`.

## Design — implement exactly this
### `core/internal/cache`
```go
type Store struct { Dir string }                    // default filepath.Join(filepath.Dir(config.Path()), "cache")
func (s Store) Load(providerID string) (snapshot.Provider, bool)
func (s Store) Save(p snapshot.Provider) error      // <dir>/<id>.json, 0600, temp+rename
```
### hybrid policy — `hybrid.Provider` gains optional fields
`Cache *cache.Store`, `ShortestWindow time.Duration`, `TokenStale func(now time.Time) bool`, `Refresh func(ctx) error`, `Fresh bool`.
Fetch order:
1. If `!Fresh && TokenStale != nil && TokenStale(now)`: load cache; if found,
   `now - ObservedAt < ShortestWindow`, and it still has a current window →
   return it with `Origin: local`, status OK. **No network.**
2. Else if `TokenStale(now)` and `Refresh != nil` → call `Refresh(ctx)`
   (errors become `NeedsSetup("<provider> sign-in is stale — open `kimi` to refresh it (auto-refresh failed: <err>)")` and fall through to local/cached).
3. Proceed as today (live ∥ local). On a usable live result, `Cache.Save`.
Registry wires: `ShortestWindow` 5h (claude, codex, kimi), 7d (grok), 30d
(deepinfra); `TokenStale` from `ParseCredentials(...).ExpiresAt` for kimi
and grok (nil for the rest); `Refresh` only for kimi.
### Kimi refresher (`providers/kimi/refresh.go`)
```go
type CLIRefresher struct { Binary string /*LookPath "kimi"*/; Startup time.Duration /*8s*/; Deadline time.Duration /*20s*/; Store CredentialStore /*to verify*/; Run func(ctx, argv []string, stdin io.Reader) error /*seam; default exec*/ }
func (r CLIRefresher) Refresh(ctx) error
```
Default `Run`: darwin → `script -q /dev/null <kimi>`; linux → `script -q -c <kimi> /dev/null`;
stdin = a reader that sleeps `Startup`, writes `/exit\r`, sleeps 2 s, then EOF;
stdout/stderr discarded; kill at `Deadline`; the run "succeeds" iff the
credential file's `expires_at` is now in the future (re-read after the run) —
the process exit code is irrelevant. `kimi` missing → NotConfigured.
### CLI
`quotamon --fresh` / `snapshot --fresh` / `waybar --fresh` set `Fresh`.
`check` never uses the cache and never triggers Refresh (report token state instead: `Kimi live: token stale (expired 12m ago) — cached reading available`).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] cache tests: round-trip, 0600, atomic
- [ ] hybrid tests with stubs: stale token + young cache → live and Refresh never called, origin local; stale token + old cache → Refresh called once, then live; Refresh error → NeedsSetup message contains "open `kimi`" and the cached/local reading is still returned when available; `Fresh` bypasses the cache; fresh token → cache ignored, live called, cache saved
- [ ] refresher test with a stub `Run` that rewrites a temp credential file with a future `expires_at` → success; stub that leaves it stale → error mentioning `kimi`; stdin script asserts the exact bytes `/exit\r` after the startup delay (use a tiny `Startup`)
- [ ] `quotamon check` shows the token-stale line for Kimi when the fixture credential is expired (temp `$HOME`)
- [ ] `core/README.md`: "Caching and refresh" paragraph — cache location, the stale-token rule, `--fresh`, and that Kimi is refreshed by launching the Kimi CLI (never by rotating tokens ourselves) — add to scope in the report
- [ ] No files changed outside scope (+ `core/README.md`)

## Out of scope
Any direct call to `auth.kimi.com`; scraping the TUI's usage box; Grok/Claude
refresh; history/pace.

## Worker report

> claimed_by: codex-1 (pid 65930) at 2026-08-29T21:53:39Z

### Verdict

Implemented and verified all acceptance criteria. QuotaMon now persists private
per-provider last readings, uses young/current cache entries for stale tokens,
launches the Kimi CLI for old/missing readings without rotating tokens itself,
supports `--fresh`, and reports stale Kimi state safely from `check`.

### Files changed

- `core/internal/cache/store.go` — added the default config-adjacent cache store with private temp-file replacement.
- `core/internal/cache/store_test.go` — covered round-trip JSON, mode `0600`, failed replacement preservation, and inode-replacing atomic saves.
- `core/internal/hybrid/provider.go` — added stale-token cache/refresh policy, cached fallback labelling, and live-result persistence.
- `core/internal/hybrid/provider_test.go` — covered every required stale/young/old/refresh-error/fresh-token/`Fresh` branch with stubs.
- `core/internal/providers/kimi/refresh.go` — added the injectable Darwin/Linux `script(1)` CLI refresher and post-run credential verification.
- `core/internal/providers/kimi/refresh_test.go` — covered future/stale credentials, ignored process exit status, exact delayed `/exit\r` input, and missing CLI classification.
- `core/internal/providers/kimi/sources.go` — added the injectable credential store used by live fetching and refresh verification.
- `core/internal/providers/kimi/kimi.go` — documented that stale-token policy stays outside the live source and never rotates Kimi tokens.
- `core/internal/providers/kimi/README.md` — documented CLI-owned refresh behavior and the no-token-rotation boundary.
- `core/internal/registry/registry.go` — wired all cache lifetimes, Kimi/Grok expiry checks, Kimi-only refresh, and `Fresh` propagation.
- `core/internal/registry/registry_test.go` — verified cache lifetimes, token/refresh wiring, and `Fresh` propagation.
- `core/cmd/quotamon/main.go` — added `--fresh`, passed policy to the registry, and added non-probing stale-token diagnostics.
- `core/cmd/quotamon/main_test.go` — covered all requested `--fresh` forms and the exact cached Kimi stale-token check line using temp state.
- `core/README.md` — added the requested **Caching and refresh** section and updated Kimi usage guidance; this is the ticket-approved scope addition.

### Commands and checks

- `bash .tigerteam/scripts/run-tests.sh` — baseline and four implementation/final runs all exited `0`.
- Final wrapper log: `.tigerteam/logs/tests/20260829-220856-9616.log`.
- Final wrapper summary:
  - `ok quotamon/cmd/quotamon (cached)`
  - `ok quotamon/internal/cache (cached)`
  - `ok quotamon/internal/hybrid (cached)`
  - `ok quotamon/internal/providers/kimi 2.310s`
  - `ok quotamon/internal/registry (cached)`
  - all other Go packages passed; Swift: `Test run with 62 tests in 1 suite passed`; `### exit 0`.
- `cd core && go run ./cmd/quotamon check` — exited `0`; Kimi reported `token stale` without launching refresh, and comparable live/local provider counts agreed with the Swift oracle. The Claude local mirror was unreadable under Seatbelt as expected.
- `swift run --disable-sandbox --package-path QuotaKit quotactl --json` — exited `0`; emitted the reference Claude/Codex snapshot. Swift's URL-cache sandbox warnings did not affect results.
- `git diff --check` — clean.

### Documentation

The new public cache, refresher, credential-store, hybrid-policy, and registry
fields/functions have Go documentation. `core/README.md` now explains cache
location, the stale-token/current-window rule, `--fresh`, and that only Kimi's
own CLI rotates its credentials. The Kimi provider README records the same
security boundary.

### Commit

- Branch: `tigerteam/T-0025`
- Commit: `f8902fa` (`[T-0025] Last-reading cache and Kimi CLI refresh`)

### Reviewer notes

- The hybrid sequence still probes live and local after an old-cache refresh
  attempt, as specified; when refresh fails, a usable local/cache fallback gets
  the explicit `open \`kimi\`` NeedsSetup message.
- The live hand-check had no saved Kimi cache yet, so it printed the stale-token
  state without the availability suffix. The exact `— cached reading available`
  form is exercised with isolated temp `$HOME` and cache state in the CLI test.
- No out-of-scope issues were changed or identified.

> handoff: review

## Rework (attempt 2)

## Rework — refresh never succeeds on the reference machine

Everything else is right (policy, no rotation, cache, `check` line, tests).
But an end-to-end run with an expired token prints
`Kimi sign-in is stale — … (auto-refresh failed: Kimi CLI did not refresh sign-in)`
and takes 10.5 s. Two causes, both verified by the PM:

1. **Working directory.** quotamon inherits the caller's cwd; the Kimi TUI
   launched there shows `Trust this folder?` and stops before refreshing.
   Launched from `$HOME` (a trusted workspace) the refresh POST happens
   within ~5 s. Fix: `cmd.Dir = os.UserHomeDir()` (error → NotConfigured with
   a message), and set `TERM=xterm-256color` in `cmd.Env` when unset. Note
   the trust prompt in the doc comment.
2. **Budget.** The refresher's 20 s deadline is derived from the CLI's 10 s
   fetch context, so the process is killed at ~10 s before the file is
   rewritten. Fix: refresh must run **before** the fetch budget starts and on
   its own clock:
   - `hybrid.Provider` gains `PreFetch(ctx context.Context)` that performs the
     stale-token → cache → `Refresh` decision (storing any cached reading /
     refresh error in the value it returns or in an unexported field on a
     returned "prepared" struct — your choice, keep it simple) and
     `Fetch` then only does live ∥ local with the prepared state.
   - `cmd/quotamon` calls `PreFetch` for all providers concurrently under
     `context.WithTimeout(background, 25 s)`, then runs the existing 10 s fetch.
     `check` never calls `PreFetch`. `--no-live` skips it.
   - Inside the refresher use `context.WithTimeout(context.WithoutCancel(ctx), Deadline)`
     so a caller's earlier cancel cannot truncate a launch mid-write; still
     honour `Deadline` (20 s) and kill on it.

### Additional acceptance
- [x] On this machine, with `~/.kimi-code/credentials/kimi-code.json` expired
      (it expires 15 min after any kimi launch — just wait), `go run
      ./cmd/quotamon` prints the Kimi block with live percentages, exits 0,
      total wall time < 25 s, and afterwards `pgrep -f 'script .*kimi'` is
      empty. **Done** — exit 0, wall **11s**, live percentages below, pgrep empty. (The sandbox
      allows reading/writing `~/.kimi-code` and execing `kimi`; the launch
      itself makes no chat request — only `/models`, `client_configs`, and
      the token refresh.)
- [x] Unit test: refresher sets `Dir` to the home dir and `TERM` when unset
      (assert via the injected `Run` receiving a `*exec.Cmd`-like struct, or
      expose the argv/dir/env through the seam). **Done** — `TestCLIRefresherRunsFromHomeAndSetsTERMWhenUnset`.
- [x] Existing criteria unchanged — all original + cache/hybrid/refresher/check criteria still pass.


## Worker report (rework / attempt 2 — ds-1)

### Verdict

Rework completed and verified. The Kimi refresher now launches the CLI from
`$HOME` (so the TUI does not stop at its "Trust this folder?" prompt) with
`TERM` set when the caller has none, and refresh runs **ahead of** the fetch
budget on its own 20 s clock (`context.WithoutCancel`) so it is no longer
killed ~10 s in, before the CLI rewrites its credential file. The two causes
called out in the rework are fixed and the machine end-to-end run passes.

### Files changed (rework, on top of attempt-1 commit `f8902fa`)

- `core/internal/providers/kimi/refresh.go` — launch from `os.UserHomeDir()`
  (error → `NotConfigured` with a message); set `TERM=xterm-256color` when
  unset; **replace the `Run` seam's signature** with `func(*exec.Cmd) error` so
  the prepared command (already carrying `Dir`, `Env`, `Stdin`) is handed to the
  seam; run on `context.WithTimeout(context.WithoutCancel(ctx), Deadline)` so the
  caller's cancel truncating a mid-write launch is impossible.
- `core/internal/hybrid/provider.go` — split the policy into `PreFetch(ctx)
  Prepared` (cache read → `Refresh` → error message) and `Prepared.Fetch(ctx)`
  (sources only); `Provider.Fetch` composes both for legacy callers.
- `core/cmd/quotamon/main.go` — `prefetchTimeout` (25 s) and `fetchTimeout`
  (10 s); `preFetchAll` runs every provider's `PreFetch` concurrently before
  `fetchAll`; `check` still calls `probeAll` directly and never `PreFetch`.
- `core/cmd/quotamon/main_test.go` — `TestCheckReportsAStaleTokenWithoutRefreshing`.
- `core/internal/hybrid/provider_test.go` — `TestPreFetchRefreshesOnceBeforeASeparateFetch`
  and `TestPreFetchSkipsTheCacheAndRefreshWhenLiveIsDisabled`.
- `core/internal/providers/kimi/refresh_test.go` — `TestCLIRefresherRunsFromHomeAndSetsTERMWhenUnset`
  plus updated seam signatures.
- `core/internal/providers/kimi/README.md`, `core/README.md` — documented the
  home-dir launch (trust prompt), the 20 s independent deadline, and that the
  refresh runs ahead of (never inside) the 10 s fetch budget.

### Commands and output — acceptance

- `bash .tigerteam/scripts/run-tests.sh` — **exit 0** after `go clean -testcache`
  (fresh, non-cached run). All packages `ok`: `cmd/quotamon 1.308s`,
  `internal/cache 0.783s`, `internal/hybrid 1.431s`, `internal/providers/kimi
  4.386s`, `internal/registry 2.365s`, others pass; Swift `62 tests in 1 suite
  passed`. `go vet ./...` clean; `git diff --check` clean.
- `cd core && go vet ./... && go build ./...` — clean.
- Machine end-to-end (token expired 23:13:11Z; I moved the young kimi cache
  aside temporarily so the refresh path actually runs, then restored it):
  `go run ./cmd/quotamon` → **exit 0**, **wall 11s (< 25s)**, and afterwards
  `pgrep -f 'script .*kimi'` **empty**. Kimi block:
  ```
  Kimi         basic          live · just now
    5h        █████████████████░░░  84%  2h 26m
    Week      ███░░░░░░░░░░░░░░░░░  17%  5d 10h
  ```
  The CLI genuinely rotated the credential (Kimi `expires_at` moved to a future
  23:30:38Z after the run) — the launch made no chat request.
- `go run ./cmd/quotamon check` — exit 0, no refresh triggered; Kimi `ok — 2
  windows, basic`. Claude local mirror unreadable under Seatbelt (expected, out
  of scope). The exact `Kimi live: token stale (expired …) — cached reading
  available` line is covered by the temp-`$HOME` CLI unit test.

### Documentation

Added the home-dir / trust-prompt rationale and the independent 20 s refresh
clock to the `CLIRefresher.Refresh` doc comment and both provider READMEs;
`core/README.md` (in-scope addition per the ticket) describes the prefetch
budget in the "Caching and refresh" section.

### Commit

- Branch: `tigerteam/T-0025`
- Commit: `0724991` (`[T-0025] Last-reading cache and Kimi CLI refresh — rework:
  prefetch budget and home-dir launch`) on top of `f8902fa`.

### Reviewer notes

- The `Run` seam now receives an already-prepared `*exec.Cmd` (Dir/Env/Stdin),
  so tests can assert the home dir and `TERM` directly — this was the cleanest
  way to satisfy the rework's unit-test criterion.
- I temporarily removed the machine's young kimi cache to force the real
  refresh/live path for the end-to-end criterion, then restored the backup;
  machine cache state is unchanged. The real Kimi credential was legitimately
  refreshed by the CLI during the run.
- No out-of-scope issues changed or introduced.

> claimed_by: kimi (pid 65930) at 2026-08-29T22:13:21Z
> RETRY: provider quota/rate limit hit at 2026-08-29T22:58:24Z (no attempt penalty)

> claimed_by: ds-1 (pid 65930) at 2026-08-29T22:58:24Z

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | codex-1 | gpt-5.6-sol | 55s | — | — | — | — | todo |
| 1 | codex-1 | gpt-5.6-sol | 16m 17s | 3.43M (3.27M) | 38.68k | — | — | review |
| 2 | grok-1 | — | 30s | — | — | — | — | todo |
| 2 | kimi | — | 45m 3s | — | — | — | — | todo |
| 3 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 19m 45s | 1.51M (1.42M) | 21.4k | — | — | review |

total: 5 attempts · 1h 22m · 4.94M in (4.69M cached) / 60.07k out · $0.00 · 3 without usage · 5 without cost (accepted 2026-08-29T23:19:55Z)
