---
id: T-0032
title: quotamon --demo renders a representative snapshot through the real table renderer
priority: P2
complexity: C1
depends_on: []
scope: [core/cmd/quotamon/, core/README.md]
---

## Goal
For documentation screenshots we need the exact console table without a real
account (privacy: never ship live percentages/balances). Add `--demo` that
feeds a built-in representative snapshot to the SAME renderer the live table
uses, so the screenshot always matches real formatting.

## Context
- Table renderer + `renderTable`/`renderWaybar` live in `core/cmd/quotamon/`
  (T-0015/T-0021). `snapshot.Snapshot` model in `core/internal/snapshot`.
- No config or network for `--demo` (it must work before setup).

## Design — implement exactly this
- `quotamon --demo` (also honoured by `snapshot --demo`, `waybar --demo`):
  build a fixed `snapshot.Snapshot` in code — Claude (max) 5h/Week/Fable wk at
  a few percents + a `20.00 (not enabled)` credit, ChatGPT (plus) 5h high/Week,
  Grok (nil plan) one Week window, DeepInfra (pay-as-you-go) balance
  `$10.03 remaining` + spend `$8.00 this month`, Kimi (basic) 5h/Week — and
  render it through the normal table/JSON/waybar path. Use a FIXED
  `generatedAt`/`observedAt`/`resetsAt` (relative to a fixed base so countdowns
  are stable, e.g. resets a fixed duration in the future via an injected `now`).
- `--demo` bypasses the config-missing gate and never touches the network,
  the keychain, or a subprocess. Exit 0.
- Colour still obeys `--color`/`NO_COLOR`.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] Test: `--demo` (via the render function over the built-in snapshot with a fixed `now`) produces a stable, asserted multi-line table containing all five providers, the `(not enabled)` credit, and `$10.03 remaining`
- [ ] Test: `--demo --json` emits valid JSON with 5 providers; `waybar --demo` one line
- [ ] `go run ./cmd/quotamon --demo` exits 0 with no config present
- [ ] `core/README.md` mentions `--demo` (one line, under usage)
- [ ] No files changed outside scope

## Out of scope
Screenshot capture itself (PM does that), the app, live data.

## Worker report

> claimed_by: codex-1 (pid 47954) at 2026-08-30T00:16:35Z

### Files changed
- `core/cmd/quotamon/demo.go` — adds the fixed, representative five-provider snapshot derived from an injected base time.
- `core/cmd/quotamon/demo_test.go` — asserts the complete stable table, five-provider JSON, one-line Waybar payload, config/provider bypass, and colour handling.
- `core/cmd/quotamon/main.go` — parses `--demo`, bypasses setup/fetching, and routes demo and live snapshots through the same output function.
- `core/README.md` — documents `quotamon --demo` in the usage block.

### Commands run
- `bash .tigerteam/scripts/run-tests.sh` (baseline) — exit 0; log `.tigerteam/logs/tests/20260830-001732-48575.log`.
- `bash .tigerteam/scripts/run-tests.sh` (first implementation run) — exit 1 only because the new golden expected one extra space after `Fable wk`; corrected the assertion.
- `gofmt -w core/cmd/quotamon/main.go core/cmd/quotamon/demo.go core/cmd/quotamon/demo_test.go && git diff --check && bash .tigerteam/scripts/run-tests.sh` — exit 0; log `.tigerteam/logs/tests/20260830-002143-50048.log`.

Final wrapper summary:
```text
cmd:  bash scripts/test-all.sh
exit: 0
ok  	quotamon/cmd/quotamon	1.808s
ok  	quotamon/internal/cache	(cached)
ok  	quotamon/internal/config	(cached)
ok  	quotamon/internal/discover	(cached)
ok  	quotamon/internal/format	(cached)
ok  	quotamon/internal/hybrid	(cached)
ok  	quotamon/internal/jsonx	(cached)
ok  	quotamon/internal/providers/claude	(cached)
ok  	quotamon/internal/providers/codex	(cached)
ok  	quotamon/internal/providers/deepinfra	(cached)
ok  	quotamon/internal/providers/grok	(cached)
ok  	quotamon/internal/providers/kimi	(cached)
ok  	quotamon/internal/registry	(cached)
ok  	quotamon/internal/snapshot	(cached)
ok  	quotamon/internal/source	(cached)
Test run with 67 tests in 2 suites passed.
### exit 0
```

- `QUOTA_MONITOR_DIR=<empty-temp-dir> go run ./cmd/quotamon --demo` from `core/` — exit 0 and printed all five providers without config.
- `QUOTA_MONITOR_DIR=<empty-temp-dir> go run ./cmd/quotamon --demo --json | jq -e '.providers | length == 5'` — exit 0, `true`.
- `QUOTA_MONITOR_DIR=<empty-temp-dir> go run ./cmd/quotamon waybar --demo` plus one-line and four-key JSON checks — exit 0, one line.
- `git diff --check` and final `git status --short` audit — no whitespace errors; all changed files were inside ticket scope.

### Documentation
- Added the requested one-line `--demo` example under `core/README.md` usage.

### Commit
- `c469003` on branch `tigerteam/T-0032` — `[T-0032] quotamon --demo renders a representative snapshot through the real table renderer`.

### Reviewer notes
- Please look closely at the representative percentages/countdowns in `demoSnapshot`; they are fixed relative to the single injected base time and intentionally reuse the production table/JSON/Waybar output path.
- No out-of-scope issues noticed or changed.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | codex-1 | gpt-5.6-sol | 6m 40s | 1.85M (1.75M) | 16.74k | — | — | review |

total: 1 attempt · 6m 40s · 1.85M in (1.75M cached) / 16.74k out · $0.00 · 1 without cost (accepted 2026-08-30T00:25:07Z)
