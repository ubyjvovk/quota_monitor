---
id: T-0074
title: Retire GO-PORT.md and stop pointing docs and the installer at quotactl
priority: P2
complexity: C1
depends_on: [T-0073]
scope: [README.md, scripts/install-claude-statusline.sh, GO-PORT.md]
---

## Goal
Two cleanups that belong together, both consequences of the Go core having won.

**1. `GO-PORT.md` goes.** It is the plan and status board for a port that
finished. The owner's call, stated directly: *"do delete the GO-PORT.md, it
serves no purpose anymore - we've progressed way too far."* Delete the file and
every live pointer to it.

**2. `quotactl` is gone** (T-0073 deleted it), but two places still send a
reader — or a user's shell — to it. One of them actually breaks.

## Context
- `scripts/install-claude-statusline.sh:48` prints:
  `echo "Open a Claude Code session, then re-run: swift run quotactl"`
  After T-0073 that command errors out. This is the one that breaks a real
  user, so treat it as the sharp end of the ticket. The replacement is the Go
  binary, `quotamon` — read `scripts/claude-statusline-mirror.sh` and the rest
  of `install-claude-statusline.sh` before choosing the wording, and make the
  printed command one the user can paste and run.
- `README.md:260` lists `Sources/quotactl/  diagnostic CLI (the parity
  reference)` in the Layout section — a directory that no longer exists.
- `README.md:312` links `[GO-PORT.md](GO-PORT.md) — the design record and
  status of the Go core port.` That link must go with the file.
- Delete the file with `git rm GO-PORT.md` so the removal is staged as a
  deletion, not left as an untracked stray.
- **`AGENTS.md` also references it twice (lines 14 and 62). Do not touch
  `AGENTS.md`** — it is the PM's file and the PM updates it when accepting this
  ticket. Note in your report that those two references are still live so the
  PM cannot miss them.
- Ticket files under `.tigerteam/board/` and entries in `.tigerteam/STATE.md`
  mention `GO-PORT.md` constantly. Those are a historical record of work as it
  happened and are **not** references to fix. Leave every one of them alone.
- `TIGERTEAM-BUILD-NOTES.md` at the repo root is untracked and not yours. Leave
  it alone.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] `bash -n scripts/install-claude-statusline.sh` exits 0
- [ ] `GO-PORT.md` no longer exists and its removal is staged as a deletion —
      show the `D GO-PORT.md` line from `git status --short`
- [ ] `grep -rn "GO-PORT" README.md scripts/ core/ omarchy/ App/ Widget/ QuotaKit/`
      returns nothing. Paste the (empty) result.
- [ ] `grep -rn "quotactl" README.md scripts/` returns nothing
- [ ] `git diff --name-only HEAD^ HEAD` lists only the three scope paths
- [ ] The command the installer now prints is correct: run it yourself and
      paste the result, or explain precisely why it cannot be run on this lane
- [ ] `README.md`'s Layout section describes the tree that now exists — no
      entry for a deleted directory, and nothing invented that is not there
- [ ] No files changed outside scope

## Out of scope
- `AGENTS.md` (PM-owned), `PROVIDERS.md`, `core/README.md`.
- `.tigerteam/` in its entirety — board tickets and STATE.md are a historical
  record, not a link graph to repair.
- `TIGERTEAM-BUILD-NOTES.md`.
- Any source file. If a doc statement is wrong about surviving code, say so in
  the report rather than changing the code.
- Preserving GO-PORT.md's content somewhere else. It is being retired, not
  migrated; git history keeps it.
- Rewriting sections of `README.md` unrelated to `quotactl`, `GO-PORT.md` or the
  Swift provider layer.

> claimed_by: codex-1 (pid 94854) at 2026-09-01T15:44:08Z

## Worker report

Verdict: ready for review.

### Files changed

- `README.md` — removed the deleted `Sources/quotactl/` Layout entry and the retired `GO-PORT.md` link.
- `scripts/install-claude-statusline.sh` — replaced the broken `swift run quotactl` follow-up with the pasteable installed-binary command `quotamon`.
- `GO-PORT.md` — deleted the completed port plan/status document with `git rm`.

### Checks performed

- `bash .tigerteam/scripts/run-tests.sh` (final run):

  ```text
  cmd:  bash scripts/test-all.sh
  exit: 0
  log:  .tigerteam/logs/tests/20260901-154633-9159.log (141 lines — grep it for specific failures; do not read it whole)
  --- result lines ---
  ok  quotamon/cmd/quotamon  2.040s
  ok  quotamon/internal/cache  (cached)
  ok  quotamon/internal/config  0.429s
  ok  quotamon/internal/discover  (cached)
  ok  quotamon/internal/format  (cached)
  ok  quotamon/internal/hybrid  (cached)
  ok  quotamon/internal/jsonx  (cached)
  ok  quotamon/internal/providers/claude  0.178s
  ok  quotamon/internal/providers/codex  (cached)
  ok  quotamon/internal/providers/deepinfra  (cached)
  ok  quotamon/internal/providers/deepseek  (cached)
  ok  quotamon/internal/providers/grok  (cached)
  ok  quotamon/internal/providers/kimi  (cached)
  ok  quotamon/internal/providers/openrouter  (cached)
  ok  quotamon/internal/providers/runinfra  (cached)
  ok  quotamon/internal/registry  0.287s
  ok  quotamon/internal/snapshot  (cached)
  ok  quotamon/internal/source  (cached)
  ✔ Test run with 51 tests in 1 suite passed after 0.029 seconds.
  omarchy model tests passed
  ### exit 0
  ```

- `bash -n scripts/install-claude-statusline.sh` exited 0 with empty output.
- Before commit, `git status --short` showed the required staged deletion:

  ```text
  D  GO-PORT.md
   M README.md
   M scripts/install-claude-statusline.sh
  ```

- `test ! -e GO-PORT.md` exited 0 after commit.
- `grep -rn "GO-PORT" README.md scripts/ core/ omarchy/ App/ Widget/ QuotaKit/` returned the required empty result (exit 1, no matches):

  ```text
  ```

- `grep -rn "quotactl" README.md scripts/` returned the required empty result (exit 1, no matches):

  ```text
  ```

- `git diff --name-only HEAD^ HEAD`:

  ```text
  GO-PORT.md
  README.md
  scripts/install-claude-statusline.sh
  ```

- `git status --short` after commit was empty; no files outside scope changed.
- Verified each directory named by README's Layout (`core/`, `core/cmd/quotamon/`, `core/internal/providers/`, `QuotaKit/`, `App/`, `Widget/`, `omarchy/`, `docs/`, and `scripts/`) exists.
- The literal installer follow-up command could not be executed on this lane: `command -v quotamon` exited 1 with empty output because the isolated worker checkout does not install a release binary globally or add one to `PATH`. The installer now prints `quotamon`, the normal pasteable invocation once the Go binary is installed; the core command package passed the wrapper's Go tests.
- `git diff --check` exited 0.

### Documentation

The root README now reflects the surviving tree and no longer points readers at the completed port record. `GO-PORT.md` was retired rather than migrated, as requested.

### Commit

- `cb317ab7cbfc937ef5e9548ad08bdd8f27398678` on `tigerteam/T-0074`

### Reviewer notes

- `AGENTS.md` still contains the two known `GO-PORT.md` references at lines 14 and 62. It is PM-owned and intentionally untouched; the PM must remove those when accepting this ticket.
- The initial pre-change wrapper run also exited 0 (`.tigerteam/logs/tests/20260901-154459-8164.log`), so there were no pre-existing failures.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | codex-1 | gpt-5.6-sol | 4m 16s | 842.42k (788.48k) | 9k | — | — | review |

total: 1 attempt · 4m 16s · 842.42k in (788.48k cached) / 9k out · $0.00 · 1 without cost (accepted 2026-09-01T15:49:46Z)
