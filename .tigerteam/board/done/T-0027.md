---
id: T-0027
title: DeepInfra — show remaining prepaid balance (payment/checklist) next to month-to-date spend
priority: P1
complexity: C2
depends_on: []
scope: [core/internal/providers/deepinfra/, core/internal/snapshot/snapshot.go, core/internal/snapshot/snapshot_test.go, core/cmd/quotamon/table.go, core/cmd/quotamon/table_test.go, core/cmd/quotamon/waybar.go, core/cmd/quotamon/waybar_test.go]
---

## Goal
DeepInfra currently prints only `spend  $7.97 this month`. The account has a
prepaid balance: `GET /payment/checklist` → `stripe_balance` (negative =
funds ready to spend). Show `balance  $18.00` as the headline and keep the
spend line. Read `PROVIDERS.md` § DeepInfra "Balance" first — including the
PII warning: the response also contains the user's postal address; pick only
the money/status fields.

## Context
- `core/internal/providers/deepinfra/sources.go` (T-0013 + T-0023: two
  parallel calls with per-call timeout — add the third the same way).
- Fixture `deepinfra-checklist.json` (money fields only).
- `snapshot.Credits` today: `HasCredits, Unlimited, Balance, Enabled`. The
  Swift decoder ignores unknown keys, so adding a field is contract-safe.

## Design — implement exactly this
- `snapshot.Credits` gains `Spend *string `json:"spend,omitempty"`` — a
  provider-formatted spend string. Doc comment: "money already spent in the
  current period, when a provider reports it separately from the balance".
- DeepInfra `Fetch` issues **three** GETs concurrently: `/payment/config`,
  `/payment/usage?from=current`, `/payment/checklist`. From checklist take
  ONLY: `stripe_balance`, `recent`, `limit`, `suspended`, `suspend_reason`,
  `overdue_invoices`, `billing_type`, `topup`. Never retain the body.
- Credits mapping:
  - `stripe_balance < 0` → `Balance: "$18.00"` (abs, `%.2f`), `HasCredits: true`, `Unlimited: false`, `Enabled: !suspended`
  - `stripe_balance == 0` → `Balance: "$0.00"`, `HasCredits: false`, `Enabled: !suspended`
  - `stripe_balance > 0` → `Balance: "$X owed"`, `HasCredits: false`
  - checklist failed but usage OK → today's behaviour (`Unlimited: true`, `Balance: "$7.97 this month"`) plus NeedsSetup("Balance unavailable — DeepInfra /payment/checklist: <err>")
  - `Spend`: `"$7.97 this month"` from usage (as today), always when usage succeeded.
  - `suspended` → status `Failed("DeepInfra account suspended: <suspend_reason>")`; `overdue_invoices > 0` → `NeedsSetup("DeepInfra has $X in overdue invoices")`.
- Windows unchanged (only with a positive `limit`).
- Table (`table.go`): when `Credits.Spend != nil` print two rows:
  `  balance   $18.00` (via the normal credits rules — enabled with balance → `$18.00 remaining`) and `  spend     $7.97 this month`. When only `Spend` exists (Unlimited+Balance path), keep the single `spend` row as today.
- Waybar tooltip: same two lines. Text stays `DI —` (no percentage is invented).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] Test on `deepinfra-checklist.json` + `deepinfra-usage.json` (limit −1): credits `{HasCredits:true, Unlimited:false, Balance:"$18.00", Enabled:true, Spend:"$7.97 this month"}`, zero windows, status ok
- [ ] Test: `stripe_balance: 5.0` → Balance `"$5.00 owed"`, HasCredits false; `suspended: true` → Failed status containing "suspended"
- [ ] httptest test: all three paths requested concurrently (each handler sleeps 300 ms; Fetch < 500 ms); checklist 500 → spend-only credits with the NeedsSetup message; a checklist body containing `billing_address_info` never appears in the encoded snapshot (assert the JSON has no "line1"/"postal_code")
- [ ] Table golden: DeepInfra block renders `balance   $18.00 remaining` then `spend     $7.97 this month`; waybar tooltip test likewise
- [ ] Swift: `cd QuotaKit && swift test` still passes (no Swift change expected; a snapshot with `spend` decodes)
- [ ] `core/internal/providers/deepinfra/README.md` + `core/README.md` DeepInfra subsection updated (balance semantics, PII rule) — add `core/README.md` to scope in the report
- [ ] No files changed outside scope (+ `core/README.md`)

## Out of scope
Top-up actions, invoices, the billing portal, Swift model changes.

## Worker report

> claimed_by: ds-1 (pid 65930) at 2026-08-29T21:58:40Z

**Files changed (all within scope + `core/README.md` as the ticket instructs):**
- `core/internal/snapshot/snapshot.go` — added `Credits.Spend *string json:"spend,omitempty"` (contract-safe; unknown keys ignored by Swift).
- `core/internal/providers/deepinfra/sources.go` — `Fetch` now issues three concurrent GETs (`/payment/config`, `/payment/usage?from=current`, `/payment/checklist`); added `balanceFromChecklist` which reads only the money/status fields and never retains the body; usage failure still returns, a sibling Unauthorized outranks it.
- `core/internal/providers/deepinfra/deepinfra.go` — new `Balance` struct (`Stripe, Known, Suspended, SuspendReason, OverdueInvoices`); `Snapshot` now takes a `Balance` and maps it per the spec (negative → spendable `$X.XX`, zero → `$0.00`, positive → `$X.XX owed`), `Spend` always `"$X.XX this month"` from usage, checklist-failed → spend-only fallback, `suspended` → `Failed`, `overdue_invoices>0` → `NeedsSetup`.
- `core/internal/providers/deepinfra/deepinfra_test.go` — updated existing tests for the new signature/third endpoint and added: fixture-combination prepaid test, `stripe_balance` mapping table, suspended/overdue status, checklist-500 fallback, and a PII test asserting `line1`/`postal_code` never appear in encoded snapshot.
- `core/cmd/quotamon/table.go` — new `creditLines` helper: prepaid balance + spend print two rows (`balance $18.00 remaining`, `spend $7.75 this month`); spend-only keeps the single `spend` row.
- `core/cmd/quotamon/waybar.go` — waybar tooltip uses the same `creditLines`, so tooltip and table agree.
- `core/cmd/quotamon/table_test.go`, `waybar_test.go` — golden/behavioural tests for the two-row balance+spend rendering and the unchanged single spend row.
- `core/internal/snapshot/snapshot_test.go` — test that `spend` encodes alongside `balance`.
- `core/internal/providers/deepinfra/README.md`, `core/README.md` — documented balance semantics (negative=prepaid, positive=owed) and the PII rule.

**Commands run:**
- `bash .tigerteam/scripts/run-tests.sh` → `### exit 0`; Swift: `✔ Test run with 62 tests in 1 suite passed` (no Swift change); Go: `go vet` + `go test` all `ok`. No pre-existing failures (baseline was green).

**Doc changes:** `core/internal/providers/deepinfra/README.md` and `core/README.md` DeepInfra subsection (balance mapping + PII rule).

**Commit:** `1f99f6e` on branch `tigerteam/T-0027`.

**Notes for the reviewer — look at closely:**
1. **Acceptance-criteria amount slip ($7.97 vs $7.75).** The ticket's acceptance literals say Spend `"$7.97 this month"`, but the design says Spend comes *from usage* ("as today") and `deepinfra-usage.json` has `total_cost: 775` cents = **$7.75**. `$7.97` is the checklist's `recent` field, which the design deliberately does not use for Spend. I followed the design/"from usage" instruction and the pre-existing `$7.75 this month` assertions → **$7.75**. Same slip in the table/waybar golden literals. This mirrors the documented T-0013-style PM slip; the design text governs.
2. `Unlimited` stays tied to `!hasLimit` (a spending ceiling), not to "balance unknown" — the fallback path keeps today's semantics, so a positive-limit window test still sees `Unlimited:false`.
3. Error/status precedence: suspended `Failed` is never overwritten by the config/checklist `NeedsSetup` messages; when both config and checklist fail the config (limit-unknown) message wins, preserving pre-balance behaviour.
4. The Swift `Credits` decoder uses standard `JSONDecoder` + explicit `CodingKeys`, so the extra JSON `spend` key is ignored — verified no Swift change is needed.

**Out-of-scope things noticed, not touched:** `PROVIDERS.md` already documents the checklist/balance endpoint and PII warning fully; no change needed there (staying in ticket scope). The core/README.md sample table still illustrates a spend-only block (a valid rendering), left as-is.

> handoff: review

## Rework (attempt 2)

## Rework — the balance formula changed after you claimed (my correction landed late)

You were right about `$7.75` (usage fixture governs; my `$7.97` literals
were a slip). One real change, verified against the user's dashboard and the
paid invoices ($8 + $10 = $18 deposited):

`stripe_balance` is the funds on account **before** the not-yet-invoiced
usage in `recent`. The remaining balance the dashboard shows is

    remaining = −stripe_balance − recent        (18.00 − 7.97 = 10.03)

So, from the checklist take `recent` as well and map:
- `remaining > 0`  → `Balance: "$10.03"`, `HasCredits: true`, `Enabled: !suspended`
- `remaining == 0` → `Balance: "$0.00"`, `HasCredits: false`
- `remaining < 0`  → `Balance: "$X.XX owed"` (abs), `HasCredits: false`
`Spend` stays `"$7.75 this month"` from usage (unchanged).

Update the fixture-combination test to `"$10.03"`, the mapping table
(e.g. `stripe_balance: 5.0, recent: 2.0` → `"$7.00 owed"`;
`stripe_balance: −8.0, recent: 7.97` → `"$0.03"`), the table/waybar goldens
(`balance   $10.03 remaining`), and the two READMEs (state the formula and
why: credit is debited only when the usage invoice is issued). `PROVIDERS.md`
already has the worked example — match it.

Done = the criteria above plus the existing ones, with the wording of the
acceptance list read as `$7.75` for spend and `$10.03` for balance.

## Rework executed (attempt 3, ds-1)

The rework formula is implemented and all criteria pass;
`bash .tigerteam/scripts/run-tests.sh` → `### exit 0`
(Swift: `✔ Test run with 62 tests in 1 suite passed`; Go: `go vet` + `go test` all `ok`).

**Additional changes on top of commit `1f99f6e`:**
- `core/internal/providers/deepinfra/deepinfra.go` — `Balance` gains `Recent`;
  mapping switch now computes `remaining := -Stripe - Recent`: `>0` → spendable
  `"$X.XX"` HasCredits, `==0` → `"$0.00"` no credits, `<0` → `"$X.XX owed"`.
- `core/internal/providers/deepinfra/sources.go` — `balanceFromChecklist` also
  reads `recent` (still never the PII/card fields).
- `core/internal/providers/deepinfra/deepinfra_test.go` — fixture-combination
  and suspended tests now assert `$10.03`; mapping table tests `$7.00 owed`
  and `$0.03`.
- `core/cmd/quotamon/table_test.go`, `waybar_test.go` — goldens now
  `balance   $10.03 remaining`.
- `core/internal/providers/deepinfra/README.md`, `core/README.md` — state the
  `−stripe_balance − recent` formula and *why* (credit debited when the usage
  invoice is issued), matching PROVIDERS.md's worked example.

**Float nuance (review closely):** `remaining == 0` uses exact equality, and
`stripe_balance: -8.0, recent: 7.97` computes `0.0300…27 > 0` in float64, so
it maps to the **prepaid** path (`HasCredits: true, "$0.03"`), which is also
what the rework's `"$0.03"` literal implies. Exact-zero is rare in practice;
if you want it to land on `"$0.00"` that needs a cents-rounding comparison.
`Unlimited` stays tied to a spending ceiling, unchanged from attempt 1; the
`$18.00` assertions left in the path-header test (`/payment/config`-vs-checklist
with no `recent` stub) are correct because `remaining = 18.0 − 0`.

**Commit:** `141a0b3` on branch `tigerteam/T-0027` (rework on top of `1f99f6e`).

> claimed_by: ds-1 (pid 65930) at 2026-08-29T22:14:51Z
> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 14m 33s | 3.21M (2.94M) | 45.85k | — | — | review |
| 2 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 4m 51s | 1.25M (1.15M) | 15.73k | — | — | review |

total: 2 attempts · 19m 24s · 4.46M in (4.09M cached) / 61.58k out · $0.00 · 2 without cost (accepted 2026-08-29T22:21:25Z)
