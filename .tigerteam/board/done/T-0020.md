---
id: T-0020
title: Table renderer omits the credits line for a disabled zero balance
priority: P2
complexity: C1
depends_on: [T-0017]
scope: [core/cmd/quotamon/table.go, core/cmd/quotamon/table_test.go]
---

## Goal
`quotamon` prints `credits 0 (not enabled)` for ChatGPT. Swift's reference
(`creditsLine` in `QuotaKit/Sources/QuotaKit/Support/ConsoleReport.swift`)
omits the line when a disabled credit has no meaningful balance; the Go
renderer should match.

## Context
- `core/cmd/quotamon/table.go` (T-0015) — the credits-line helper.
- Rule set for reference: `unlimited` with balance → balance verbatim;
  `unlimited` without → "unlimited"; enabled with balance → "<balance>
  remaining"; disabled with a non-zero balance → "<balance> (not enabled)".

## Design — implement exactly this
A credit with `Enabled == false` whose `Balance` is nil, empty, or parses as
numerically zero (`"0"`, `"0.00"`, `"0,00"` — trim currency symbols/spaces
before parsing) prints **no** credits line. Everything else unchanged.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] Table test cases: disabled + `"0"` → no line; disabled + `"20.00"` → `20.00 (not enabled)`; disabled + nil → no line; enabled + `"0"` → `0 remaining` (enabled zero is still shown)
- [ ] Doc comment on the helper states the rule
- [ ] No files changed outside scope

## Out of scope
Waybar tooltip, hybrid, providers.

## Worker report

> claimed_by: codex-2 (pid 65930) at 2026-08-29T20:39:25Z

### Files changed

- `core/cmd/quotamon/table.go` — suppress disabled nil, blank, and numerically zero balances after trimming surrounding currency symbols/spaces; preserve every other credits rendering rule.
- `core/cmd/quotamon/table_test.go` — add table-driven coverage for disabled zero forms, disabled non-zero/nil balances, and enabled zero balances.

### Commands and verification

- Baseline: `bash .tigerteam/scripts/run-tests.sh`
  - `exit: 0`
  - `✔ Test run with 62 tests in 1 suite passed after 0.012 seconds.`
  - `ok  quotamon/cmd/quotamon  1.673s`
  - `### exit 0`
- Formatting and diff validation: `gofmt -w core/cmd/quotamon/table.go core/cmd/quotamon/table_test.go && git diff --check -- core/cmd/quotamon/table.go core/cmd/quotamon/table_test.go`
- Final: `bash .tigerteam/scripts/run-tests.sh`
  - `exit: 0`
  - `✔ Test run with 62 tests in 1 suite passed after 0.007 seconds.`
  - `ok  quotamon/cmd/quotamon  0.336s`
  - `### exit 0`
- Acceptance audit: the focused Go table test covers disabled `"0"`, `"0.00"`, currency/spaces plus `"0,00"`, `"20.00"`, nil, and enabled `"0"`; `git diff --name-only` listed only the two scoped files before commit; `git status --short` was empty after commit.

### Documentation

- Added the requested `tableCredits` doc comment stating that disabled absent, blank, or numerically zero balances are omitted after currency-symbol/space trimming.

### Commit

- `dbcad15ef5dc46cd9124e9326b196fadb08f9de8` on `tigerteam/T-0020`

### Reviewer notes

- Zero detection normalizes a decimal comma only for parsing; displayed non-zero balances retain their original text. No out-of-scope issues were observed or changed.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | codex-2 | gpt-5.6-sol | 2m 40s | 403.38k (350.46k) | 6.12k | — | — | review |

total: 1 attempt · 2m 40s · 403.38k in (350.46k cached) / 6.12k out · $0.00 · 1 without cost (accepted 2026-08-29T20:43:32Z)
