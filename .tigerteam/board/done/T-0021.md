---
id: T-0021
title: Table output — rune-aware aligned columns, inline usage bars, optional colour
priority: P1
complexity: C2
depends_on: []
scope: [core/cmd/quotamon/table.go, core/cmd/quotamon/table_test.go, core/cmd/quotamon/main.go, core/cmd/quotamon/main_test.go, core/internal/format/, core/README.md]
---

## Goal
The bare `quotamon` table is functional but misaligned and hard to scan. The
user chose: **aligned columns + an inline usage bar per window + optional
colour**. Current defects to fix on the way: padding counts bytes so the `—`
plan placeholder shifts the row; `pay-as-you-go` overflows the plan column;
providers run together with no separator; `check` prints "1 windows".

## Context
- Renderer: `core/cmd/quotamon/table.go` (T-0015/T-0020). Formatting helpers:
  `core/internal/format/`. Keep the credits-line rules from T-0020 intact.
- Current output on the reference machine (bytes-padded, note the shifted Grok row):
  ```
  Claude          max    live · just now
    Fable wk          23%  resets in 1d 16h
  Grok            —      live · just now
  DeepInfra       pay-as-you-go live · just now
  ```
- Severity thresholds are shared with waybar/Swift: <70 normal, <90 warning,
  ≥90 critical (`severity()` in `waybar.go` — reuse, do not duplicate the numbers).

## Design — implement exactly this
Layout (widths in terminal cells; pad with `utf8.RuneCountInString`, never `len`):
```
Claude       max            live · just now
  Fave wk   ████▓░░░░░░░░░░░░░░░  23%  1d 16h
  Week      ███░░░░░░░░░░░░░░░░░  15%  1d 16h
  5h        █░░░░░░░░░░░░░░░░░░░   6%  2h 39m
  credits   20.00 (not enabled)

ChatGPT      plus           live · just now
  5h        ████████████████████ 100%  8m
  Week      ██████░░░░░░░░░░░░░░  31%  5d 11h

Grok         —              live · just now
  Week      █████████████░░░░░░░  63%  2d 13h

DeepInfra    pay-as-you-go  live · just now
  spend     $7.96 this month
```
- Header: name left-padded to 12, plan to 14 (`—` when nil; never truncate —
  if longer, let it push the origin column right for that row only), then
  `<origin word> · <age>`. Blank line between providers; none after the last.
- Window row: 2-space indent, label padded to 9 (truncate with `…` at 9),
  a **20-cell bar**, percent right-aligned in 4 cells (`100%`), then the
  countdown (`format.Countdown`, without the words "resets in"; `—` when no
  reading; `reset` when the window has rolled over).
- Bar: `filled = round(pct/5)`, capped at 20; filled cells `█`, empty `░`;
  no reading → 20 × `░` and percent `—`. Percent over 100 fills all 20.
- Credits row: label `credits` (or `spend` when `Unlimited && Balance != nil`,
  i.e. the DeepInfra case), then the T-0020 text rules.
- `check`: pluralise correctly (`1 window`, `2 windows`).

Colour (default **auto**): `--color=auto|always|never` flag and `NO_COLOR`
env (any value → never; see no-color.org). `auto` = on only when stdout is a
terminal (`os.Stdout.Stat()` mode has `os.ModeCharDevice`). When on, colour
**only the filled bar cells and the percent** by severity: normal → default
colour (no SGR), warning → `\x1b[33m`, critical → `\x1b[31m`; reset with
`\x1b[0m`. Nothing else is coloured. Colour never changes column widths.
Put the ANSI logic in `core/internal/format/color.go` (`Colorize(sev, s)`),
so waybar/JSON never see escapes.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] Golden test with colour off: the exact block above (fixed `now`, hand-built snapshot incl. a nil-plan provider, a rolled-over window, a `pay-as-you-go` plan, a `(not enabled)` credit, a `$x this month` spend) — assert the literal string; every row's visual width computed by rune count is consistent (test asserts the percent column starts at the same rune offset on every window row)
- [ ] Colour tests: `--color=always` wraps filled cells + percent in `\x1b[31m…\x1b[0m` at 100% and `\x1b[33m` at 75%, nothing at 30%; `NO_COLOR=1` beats `--color=always`; `--color=never` emits no `\x1b`
- [ ] `check` test: "1 window" / "2 windows"
- [ ] `--json`, `snapshot`, `waybar` output unchanged (existing tests still pass, no `\x1b` anywhere in them)
- [ ] `core/README.md`: sample table replaced with the new layout; `--color` and `NO_COLOR` documented in one paragraph
- [ ] No files changed outside scope

## Out of scope
Sparklines/history, truecolour, Windows console handling, changing the
severity thresholds.

## Worker report

> claimed_by: codex-1 (pid 65930) at 2026-08-29T21:24:28Z

The PM resolved both specification contradictions in favour of the committed
implementation: 23% uses five full cells, and rollover remains a separate
literal test. The implementation is complete and its full suite passes.

Files changed:
- `core/cmd/quotamon/table.go` — added rune-aware columns, 20-cell usage bars,
  compact countdowns, provider separators, and `spend` credit labels.
- `core/cmd/quotamon/table_test.go` — added the literal aligned golden, rune
  offset assertion, and separate rolled-window/rune-truncation coverage.
- `core/cmd/quotamon/main.go` — added colour flag parsing, `NO_COLOR`/TTY
  policy, table-only colour enabling, help text, and window pluralisation.
- `core/cmd/quotamon/main_test.go` — added colour/precedence, machine-readable
  ANSI exclusion, invalid-flag, and `1 window`/`2 windows` tests.
- `core/internal/format/color.go` — added the documented ANSI `Colorize`
  helper for warning and critical severities.
- `core/README.md` — replaced the sample table and documented `--color` and
  `NO_COLOR` in one paragraph.

Commands run:
- `bash .tigerteam/scripts/run-tests.sh` (baseline): `exit: 0`; Swift `Test run
  with 62 tests in 1 suite passed`; all Go packages `ok`; `### exit 0` (log:
  `.tigerteam/logs/tests/20260829-212449-93178.log`).
- `gofmt -w core/cmd/quotamon/table.go core/cmd/quotamon/table_test.go
  core/cmd/quotamon/main.go core/cmd/quotamon/main_test.go
  core/internal/format/color.go`.
- `bash .tigerteam/scripts/run-tests.sh` (final): `exit: 0`; Swift `Test run
  with 62 tests in 1 suite passed`; `ok quotamon/cmd/quotamon 0.391s`; `ok
  quotamon/internal/format 0.756s`; every remaining Go package passed or was
  cached; `### exit 0` (log:
  `.tigerteam/logs/tests/20260829-213030-94942.log`).
- `bash .tigerteam/scripts/run-tests.sh` (post-answer revalidation): `exit: 0`;
  Swift `Test run with 62 tests in 1 suite passed`; all Go packages passed or
  were cached; `### exit 0` (log:
  `.tigerteam/logs/tests/20260829-213513-96822.log`).
- `git diff --check HEAD^ -- <scoped files>` completed with no output;
  `git diff --name-only HEAD^` listed only the six scoped files below.

Documentation: the README now contains the aligned bar layout and a single
paragraph covering auto/always/never colour plus `NO_COLOR` precedence.

Commit: `21d1465dade82302759d55bae14e9525b723387b` on
`tigerteam/T-0021` (`[T-0021] wip: aligned table and colour support`).

Reviewer note: per the PM's answers, 23% renders as five `█` cells and
rollover is tested separately from the exact golden. No files outside scope
were changed; the worktree is clean at the recorded commit.

## Questions

1. The exact required block renders 23% as `████▓░…`, but the bar rule says
   `filled = round(pct/5)` and permits only filled `█` and empty `░` cells.
   Should 23% render as five `█` cells (current implementation, recommended
   because it follows the explicit algorithm), or should partial `▓` cells be
   introduced? If partial cells are intended, please specify their rounding and
   whether they receive severity colour.
2. The golden criterion says its hand-built snapshot must include a rolled-over
   window, but the exact required block contains no `reset` row. Should rollover
   remain a separate literal unit test (current implementation, recommended so
   the golden stays exact), or should a reset row be added to the golden? If it
   belongs in the golden, please provide its provider/label and placement.

## Answers

PM answer — both as you recommend:
1. The rule wins: `filled = round(pct/5)`, only `█` and `░`. The `▓` in the
   golden block was a typo; 23% renders as five `█`. Treat the golden block
   as corrected accordingly.
2. Keep rollover as its own literal unit test; the golden stays exact and
   need not contain a `reset` row.
Your committed implementation already matches — run the suite once more and
hand off to review.


> claimed_by: codex-1 (pid 65930) at 2026-08-29T21:34:28Z

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | codex-1 | gpt-5.6-sol | 8m 23s | 808.46k (735.23k) | 22.18k | — | — | blocked |
| 2 | codex-1 | gpt-5.6-sol | 1m 36s | 282.33k (234.75k) | 3.79k | — | — | review |

total: 2 attempts · 9m 59s · 1.09M in (969.98k cached) / 25.97k out · $0.00 · 2 without cost (accepted 2026-08-29T21:37:05Z)
