---
id: T-0029
title: Go core — machine-readable setup surface (discover --json, config get/set/path)
priority: P1
complexity: C2
depends_on: []
scope: [core/cmd/quotamon/, core/internal/config/, core/internal/discover/]
---

## Goal
The macOS app (and any GUI) must run first-run setup without a terminal. Give
the core machine-readable commands so a GUI can list providers, read config,
and write config through the core (keeping 0600 + validation in one place).
The interactive `quotamon setup` wizard stays as-is for terminal users.

## Context
- `core/internal/discover/discover.go` — `All() []Finding` already exists
  (Found, Supported, Detail, Hint, NeedsKey, ID, DisplayName). Add JSON output.
- `core/internal/config/config.go` — `Config`, `Provider{Enabled, Live, APIKey}`,
  `Load()` (ErrMissing when absent), `Save()` (atomic, 0600), `Default()`,
  `Path()`. Reuse; do not change the on-disk shape.
- CLI dispatch + flag parsing in `core/cmd/quotamon/main.go`; `setup`/`providers`
  already bypass the mandatory-config gate — the new commands must bypass it too
  (they exist to create/inspect config).

## Design — implement exactly this
New subcommands (each bypasses the config-missing gate; none hits the network):
- `quotamon discover --json` → the `discover.All()` findings as a JSON array,
  keys `id, displayName, found, supported, detail, hint, needsKey`, in
  registry order. Without `--json`, keep/print the existing human table
  (reuse `providers`' renderer or discover's table; do not require a config).
- `quotamon config path` → prints `config.Path()` and a newline; exit 0.
- `quotamon config get [--json]` → `--json` prints the effective config
  (Default() merged with the file when present) as JSON with sorted keys;
  without `--json`, a short `id: on|off (detail)` table. Missing file is not
  an error here — print the defaults and exit 0.
- `quotamon config set <provider> [--enabled=<bool>] [--api-key=<v>] [--live=<mode>]`
  → load (or Default() when ErrMissing), apply only the flags given to that
  provider's entry (creating it), then `Save()`. Unknown provider id → error
  to stderr, exit 2. `--live` only valid for `codex` (values app-server|http|off)
  → else exit 2. At least one flag required → else exit 2. On success print
  `wrote <path>` and exit 0. Never echo the api-key value back.
- Unknown `config` subcommand or bad flag → usage on stderr, exit 2.

Add a `MarshalableFinding`/JSON tags on `discover.Finding` (or a small DTO in
the command) — do not leak internal-only fields. Keep `discover.All` local-only.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] `discover --json` test (temp `$HOME`, stubbed security runner): valid JSON array, one object per provider in registry order, every documented key present, no network/subprocess beyond the injected probes
- [ ] `config path` prints `config.Path()` honouring `QUOTA_MONITOR_DIR`
- [ ] `config get --json` on a temp dir with no file prints the defaults (all providers, enabled=false) and exit 0; with a file, prints its merged content; output parses as JSON with sorted keys
- [ ] `config set deepinfra --enabled=true --api-key=SECRET` writes a 0600 file with that entry; a second `config set claude --enabled=true` preserves the deepinfra entry (load-modify-save); stdout never contains `SECRET`
- [ ] `config set codex --live=bogus` → exit 2; `config set nosuch --enabled=true` → exit 2; `config set claude` (no flags) → exit 2
- [ ] `core/README.md` Configuration section documents the four commands (add to scope in the report)
- [ ] No files changed outside scope (+ `core/README.md`)

## Out of scope
The interactive wizard (unchanged), the Swift app, networking, editing the
config on-disk schema.

## Worker report

> claimed_by: codex-2 (pid 43127) at 2026-08-30T00:04:10Z

### Verdict

Implemented the complete machine-readable setup surface and verified every
acceptance path. The interactive setup wizard retains its prior observable
behavior, including omitting providers discovery marks unsupported.

### Files changed

- `core/cmd/quotamon/machine.go` — added `discover` and nested `config`
  handlers, effective-config merging, validation, stable output, and private
  load-modify-save behavior.
- `core/cmd/quotamon/machine_test.go` — covered discovery JSON shape/order,
  config path/default/merge output, 0600 writes, key redaction, preservation,
  and invalid invocations.
- `core/cmd/quotamon/main.go` — documented and dispatched the new commands
  before the mandatory-config gate with an injected discovery seam.
- `core/cmd/quotamon/setup.go` — preserved the wizard's existing omission of
  unsupported providers now that `Default()` exposes the complete catalog.
- `core/internal/config/config.go` — made `Default()` return every known
  provider disabled without changing the serialized schema.
- `core/internal/config/config_test.go` — updated default-config coverage and
  retained stable/private round-trip checks.
- `core/internal/discover/discover.go` — added the seven documented JSON tags
  to `Finding` with no internal fields exposed.
- `core/README.md` — documented `discover`, `config path`, `config get`, and
  `config set` in Configuration (explicitly added to ticket scope).

### Commands and verification

- Baseline `bash .tigerteam/scripts/run-tests.sh`: `exit: 0`; Swift: `Test run
  with 62 tests in 1 suite passed`; Go result lines: all packages `ok`.
- First implementation `bash .tigerteam/scripts/run-tests.sh`: `exit: 1`;
  existing setup compatibility test caught an unsupported default entry. The
  wizard compatibility handling was added before proceeding.
- Post-fix `bash .tigerteam/scripts/run-tests.sh`: `exit: 0`; Go result lines
  included `ok quotamon/cmd/quotamon`, `ok quotamon/internal/config`, and `ok
  quotamon/internal/discover`; all other packages passed or had no test files.
- Final `gofmt -w ... && git diff --check && bash
  .tigerteam/scripts/run-tests.sh`: `exit: 0`; log
  `.tigerteam/logs/tests/20260830-001148-46138.log`; Swift: `Test run with 62
  tests in 1 suite passed`; Go: every package `ok` (fixtures had no test files).

### Documentation

`core/README.md` now describes all four commands, missing-file defaults,
effective merging, human versus JSON output, accepted Codex live modes, atomic
0600 writes, key clearing, and the guarantee that `config set` does not echo
key material.

### Commit

- `3089dc4` on branch `tigerteam/T-0029`

### Review notes

Please look closely at the deliberate interaction between the complete
`config.Default()` catalog and `runSetup`'s pruning of findings marked
unsupported; this keeps the terminal wizard unchanged while giving GUIs all
provider IDs. No out-of-scope issues were found or changed.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | codex-2 | gpt-5.6-sol | 8m 28s | 1.45M (1.36M) | 20.64k | — | — | review |

total: 1 attempt · 8m 28s · 1.45M in (1.36M cached) / 20.64k out · $0.00 · 1 without cost (accepted 2026-08-30T00:14:07Z)
