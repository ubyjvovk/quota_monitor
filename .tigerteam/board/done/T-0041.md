---
id: T-0041
title: Stress review (codex) — independent full-repo review, report only
priority: P1
complexity: C3
assignee: codex
depends_on: []
scope: [.tigerteam/reviews/2026-08-31-codex.md]
---

## Goal
An independent, adversarial review of the whole repository by you alone.
Your deliverable is ONE markdown report at
`.tigerteam/reviews/2026-08-31-codex.md` — you change NO other file. Three
other workers are running the same brief in parallel; do not coordinate, do
not hedge toward consensus — the PM consolidates afterwards and disagreement
is useful signal.

## Context
- Orient with AGENTS.md, README.md, GO-PORT.md, PROVIDERS.md first.
- Sweep, in this order of effort: `core/` (Go — providers, snapshot contract,
  CLI), `QuotaKit/Sources/` (Swift — models, engine, ConsoleTable, UI),
  `omarchy/` (QML/JS plugin), `App/` + `Widget/` (SwiftUI — read-only for
  every lane), `scripts/`, `.github/workflows/release.yml`. Skip `docs/`,
  generated dirs, and `.tigerteam/` internals (board/logs are noise).
- You may run `bash .tigerteam/scripts/run-tests.sh` (do it once, first) and
  read-only git commands. No network. Do not fix anything — report only.

## What to hunt (in priority order)
1. Correctness bugs a user would hit: wrong numbers, wrong ordering, wrong
   state after errors, time/reset-boundary handling, nil/empty-payload paths.
2. Credential/secret handling: anything that could log, leak, or mis-scope a
   token or API key; command construction; file modes.
3. Contract drift between surfaces (console table vs macOS panel vs widget vs
   waybar vs omarchy) — the repo's rule is one derived layer, transcribed.
4. Concurrency/lifecycle: the Swift engine's refresh loop, QML process
   handling, cache read/write races.
5. Failure honesty: errors swallowed, staleness presented as live, "0%" where
   "no reading" is meant.
Not wanted: style, naming, formatting, hypothetical perf, TODO archaeology.

## Report format (exact section names)
```
# Stress review — codex — 2026-08-31
## Summary            (≤5 sentences, your overall verdict)
## Findings           (5–15, ranked most severe first)
### F1 — <short title>
- file: <path>:<line>
- severity: high|medium|low   confidence: certain|likely|possible
- claim: <one sentence>
- scenario: <concrete input/state → wrong outcome>
- fix: <one line>
## What I could not verify   (mandatory — name the things you had to assume)
## Test run                  (the run-tests.sh tail: suite counts + exit)
```
Every finding cites a real file:line you actually read. A finding you could
not confirm in code goes in with confidence "possible", not omitted.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (baseline, unmodified tree)
- [ ] The report exists at exactly `.tigerteam/reviews/2026-08-31-codex.md` with all five sections and 5–15 findings, each carrying file:line, severity, confidence, scenario, fix
- [ ] `git status` on your branch shows ONLY that one new file (no other changes)
- [ ] No files changed outside scope

## Out of scope
Fixing anything, editing any source/doc file, other workers' reports,
.tigerteam/board, network access.

## Worker report

> claimed_by: codex-1 (pid 83690) at 2026-08-31T14:26:01Z

### Files changed

- `.tigerteam/reviews/2026-08-31-codex.md` — added the requested independent stress-review report with 15 ranked findings across the named repository surfaces.

### Commands and verification

- `bash .tigerteam/scripts/run-tests.sh` — baseline run on the unmodified tree; **exit 1**. Wrapper log: `.tigerteam/logs/tests/20260831-142628-91209.log`.

```text
✔ Test run with 83 tests in 2 suites passed after 0.008 seconds.
ok  quotamon/cmd/quotamon
ok  quotamon/internal/cache
ok  quotamon/internal/config
ok  quotamon/internal/discover
ok  quotamon/internal/format
ok  quotamon/internal/hybrid
ok  quotamon/internal/jsonx
ok  quotamon/internal/providers/claude
--- FAIL: TestRunAppServerKeepsStdinOpenUntilRateLimitsReply (3.00s)
    codex_test.go:94: context deadline exceeded
FAIL  quotamon/internal/providers/codex
ok  quotamon/internal/providers/deepinfra
ok  quotamon/internal/providers/grok
ok  quotamon/internal/providers/kimi
ok  quotamon/internal/registry
ok  quotamon/internal/snapshot
ok  quotamon/internal/source
omarchy model tests passed
### exit 1
```

- PM quiet-host verification — targeted `TestRunAppServerKeepsStdinOpenUntilRateLimitsReply -count=10`; **10/10 passed in 1.3s** (`ok quotamon/internal/providers/codex`). Per the PM's answer below, this and the otherwise-green worker wrapper run satisfy the baseline criterion; no worker retry was performed.
- Read-only `rg`, `sed`, and numbered source inspection covered `core/`, `QuotaKit/Sources/`, `omarchy/`, `App/`, `Widget/`, `scripts/`, and `.github/workflows/release.yml` in the ticket's requested order; no network was used.
- `git diff --check` and report-field counts verified 15 findings and 15 each of file, severity/confidence, scenario, and fix fields.
- `git status --short --untracked-files=all` before commit showed only `.tigerteam/reviews/2026-08-31-codex.md`; no files outside scope changed.

### Documentation

- No source documentation was changed; the sole Markdown report is the ticket deliverable.

### Commit

- `cc8993b` (`tigerteam/T-0041`) — `[T-0041] Stress review codex report`
- `fa61017` (`tigerteam/T-0041`) — `[T-0041] Record quiet-host test verification`

### Reviewer notes

- Review F1–F5 first: they cover config data loss, API-key argv exposure, the app/core Kimi timeout mismatch, false-zero DeepInfra parsing, and rejection of valid credits-only snapshots.
- F6 calls out a direct policy conflict: `PROVIDERS.md` forbids launching Kimi while the registry and `core/README.md` enable/document it.
- The signed-release checksum finding is static shell analysis because signing/notarization could not be exercised in this lane.
- Acceptance criteria 1–4 are satisfied; criterion 1 relies on the PM-authorized combination of the otherwise-green worker wrapper run and the quiet-host 10/10 targeted reproduction.

## Questions

The baseline acceptance criterion is blocked by `TestRunAppServerKeepsStdinOpenUntilRateLimitsReply` timing out after three seconds. The ticket required the wrapper to be run once, first, so I did not hide the failure with a retry. The available options are (1) have review rerun the wrapper and treat a green run as evidence of a load-sensitive test, or (2) reproduce the failure and open a focused Codex app-server lifecycle ticket. I recommend reviewing the completed report now and using option 2 if the reviewer sees the same failure.

> handoff: blocked

## Answers

Option 1, with the reproduction already done for you: the PM reran
`TestRunAppServerKeepsStdinOpenUntilRateLimitsReply` targeted, `-count=10`, on
the quiet host — 10/10 pass in 1.3s (`ok quotamon/internal/providers/codex`).
Your baseline ran while FOUR review workers were saturating the machine; the
test's 3-second budget is load-sensitive, not broken. The PM will open a
focused deflake ticket in the consolidation pass — do NOT open or fix one
yourself.

For your acceptance criterion: treat the baseline as satisfied by the PM's
quiet-host run plus your own observation (document both in your report's
"Test run" section). Commit your completed report at
`.tigerteam/reviews/2026-08-31-codex.md` (report file only, nothing else) and
hand off to review.


> claimed_by: codex-1 (pid 83690) at 2026-08-31T14:46:22Z

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | codex-1 | gpt-5.6-sol | 17m 39s | 5.94M (5.57M) | 22.61k | — | — | blocked |
| 2 | codex-1 | gpt-5.6-sol | 3m 13s | 315.88k (269.06k) | 4.67k | — | — | review |

total: 2 attempts · 20m 52s · 6.26M in (5.83M cached) / 27.28k out · $0.00 · 2 without cost (accepted 2026-08-31T14:50:46Z)
