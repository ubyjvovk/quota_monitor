---
id: T-0051
title: DeepInfra: invoice count is a count; missing money fields are unknown, not $0.00
priority: P2
complexity: C2
depends_on: []
scope: [core/internal/providers/deepinfra/]
---

## Goal
Two defects (opus F8, codex F4 — see the review reports): the overdue-invoice
COUNT renders as `$%d`, and absent/malformed money fields default to zero, so
endpoint drift shows a live `$0.00 this month` instead of "malformed".

## Design — implement exactly this
- Status message: `DeepInfra has %d overdue invoice(s)` (count, no `$`).
- `sources.go`: a months[0] entry WITHOUT a valid `total_cost` float →
  `source.Errorf(source.Malformed, ...)` (same as the missing-months path),
  never a zero spend. Checklist money fields: each absent/malformed component
  degrades that component to unknown (balance omitted), never to $0.00 —
  keep components that did parse.
- Tests: `{"months":[{}]}` → Malformed; checklist without balance fields →
  provider still reports spend but no balance; overdue message text pinned.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] The three tests above exist and pass; `grep -n '\$%d' core/internal/providers/deepinfra/` prints nothing
- [ ] No files changed outside scope

## Out of scope
hybrid, registry, PROVIDERS.md (PM owns docs this wave).

## Worker report

> claimed_by: luna (pid 83690) at 2026-08-31T14:57:37Z

- `core/internal/providers/deepinfra/sources.go` — reject missing/malformed usage `total_cost` as `source.Malformed`; track checklist money-field validity.
- `core/internal/providers/deepinfra/deepinfra.go` — omit derived balance unless both checklist money components are valid; correct overdue count status text.
- `core/internal/providers/deepinfra/deepinfra_test.go` — add coverage for malformed usage, unknown checklist money, parsed-component retention, and exact overdue text; update spend-only expectations.
- `core/internal/providers/deepinfra/README.md` — document malformed usage and unknown checklist-money behavior.

Commands run:

- `bash .tigerteam/scripts/run-tests.sh`
  - `### exit 0`
  - `ok quotamon/internal/providers/deepinfra (cached)`
  - `✔ Test run with 83 tests in 2 suites passed`
  - `### node (omarchy model)` / `omarchy model tests passed`
- `rg -n '\$%d' core/internal/providers/deepinfra` — no matches.
- `git diff --check` — passed.

Commit: `7df829a` on `tigerteam/T-0051`.

Docs changed: the scoped DeepInfra README now states that invalid usage cost is malformed and incomplete checklist money omits balance while preserving spend and parsed components.

Reviewer note: no out-of-scope issues found.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | luna | gpt-5.6-luna | 5m 48s | 1.02M (949.25k) | 14.08k | — | — | review |

total: 1 attempt · 5m 48s · 1.02M in (949.25k cached) / 14.08k out · $0.00 · 1 without cost (accepted 2026-08-31T15:05:14Z)
