---
id: T-0058
title: Versioning switches to {major}.{commit count} — release script computes the tag, build.sh stamps local builds
priority: P2
complexity: C1
depends_on: []
scope: [scripts/release.sh, scripts/build.sh, README.md]
---

## Goal
The product owner is dropping semantic versioning: versions are now
`{major}.{commit count on master}` (e.g. `0.264`). The release workflow needs
NO change (it already strips `v` from any tag and uses the rest verbatim);
what is missing is a way to compute and cut such a tag, and local builds
stamping the same shape.

## Context
- `.github/workflows/release.yml`: `push: tags: v*` → `VERSION=${GITHUB_REF_NAME#v}`
  → `MARKETING_VERSION="$VERSION"`. Works for `v0.264` as-is — do not touch it.
- `scripts/build.sh` builds Debug locally; `project.yml` pins
  `MARKETING_VERSION: 0.1.0` (leave project.yml alone — override on the
  xcodebuild command line).
- Current release is `v0.2.0`; commit counts are already well above 2, so the
  next computed tag sorts after it numerically and no migration is needed.

## Design — implement exactly this
### `scripts/release.sh` (new, executable)
- `MAJOR=0` at the top with a comment: bumped by hand on breaking rewrites;
  the minor is `git rev-list --count HEAD`.
- Refuses (message, exit 1) when: not on master, working tree dirty, or the
  computed tag already exists.
- Computes `TAG="v${MAJOR}.$(git rev-list --count HEAD)"`, prints what it will
  do, tags annotated (`git tag -a "$TAG" -m "Quota Monitor ${TAG#v}"`), pushes
  `git push origin master "$TAG"`, prints the Actions URL hint.
- `--dry-run` prints the tag and exits 0 without tagging.

### `scripts/build.sh`
- Compute `VERSION="${MAJOR_DOT_COUNT}"` the same way (duplicate the two
  lines; a shared lib file is overkill) and pass
  `MARKETING_VERSION="$VERSION"` on the xcodebuild invocation, so the About
  box of a local build shows the real commit-count version instead of 0.1.0.

### `README.md`
- The Releases bullet in `## Contributing / for agents` now says: cut a
  release with `scripts/release.sh` (version = `{major}.{commit count}`,
  major bumped by hand); tags still trigger `.github/workflows/release.yml`.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] `bash scripts/release.sh --dry-run` on your branch prints a `v0.<count>` tag and exits 0 WITHOUT creating a tag (`git tag -l` unchanged — show both in the report); on a dirty tree it refuses (touch a file to show it, then restore)
- [ ] `grep -n "MARKETING_VERSION" scripts/build.sh` shows the override; `git diff --stat` shows project.yml and .github untouched
- [ ] `grep -n "release.sh" README.md` matches
- [ ] No files changed outside scope

## Out of scope
`.github/workflows/release.yml`, `project.yml`, retro-tagging old releases,
any push from your lane (release.sh pushes only when the OWNER runs it).

## Worker report

> claimed_by: ds-1 (pid 83690) at 2026-08-31T16:37:09Z

**Commit:** `d4972fd` on branch `tigerteam/T-0058` (isolated checkout).

**Files changed (scope only):**
- `scripts/release.sh` (new, executable) — computes `TAG="v${MAJOR}.$(git rev-list --count HEAD)"` (MAJOR=0, hand-bumped), refuses non-master / dirty tree / existing tag on a real cut, annotated-tags + pushes master and the tag, prints the Actions URL; `--dry-run` prints the tag and exits 0 from any branch (still refuses a dirty tree).
- `scripts/build.sh` — duplicates the MAJOR/count lines and passes `MARKETING_VERSION="$VERSION"` on the xcodebuild invocation so local Debug builds show the real commit-count version instead of project.yml's `0.1.0`. `project.yml` untouched (override on the command line, per ticket).
- `README.md` — the Releases bullet under `## Contributing / for agents` now points at `scripts/release.sh` (=`{major}.{commit count}`, major hand-bumped; `v*` tags still trigger `.github/workflows/release.yml`).

**Acceptance criteria — each executed:**
- [x] `bash .tigerteam/scripts/run-tests.sh` exits 0 — summary: `### swift test (QuotaKit)` `Test run with 91 tests in 2 suites passed after 0.031 seconds`; `### go test (core)` all `ok`; `### exit 0`.
- [x] `bash scripts/release.sh --dry-run` on the clean branch prints `v0.266` and exits 0; `git tag -l` unchanged (`v0.2.0` before and after). On a dirty tree (`touch dirty_probe.txt`) it refuses (`error: working tree is dirty…`, exit 1); probe removed, tree restored clean.
- [x] `grep -n "MARKETING_VERSION" scripts/build.sh` → `46:  MARKETING_VERSION="$VERSION" build 2>&1 \`. `git show --stat HEAD` shows only `README.md`, `scripts/build.sh`, `scripts/release.sh` — `project.yml` and `.github` untouched.
- [x] `grep -n "release.sh" README.md` → `268:- **Releases:** cut a release with `scripts/release.sh`…`.
- [x] No files changed outside scope (commit lists exactly the three scope files; `git status` clean after test probe removal).

**Doc changes:** README.md Releases bullet rewritten per ticket; build.sh/release.sh carry explanatory comments (why MAJOR is 0, why dry-run refuses a dirty tree, why local builds stamp the version).
**Reviewer notes:** The real `git push origin master "$TAG"` path was not executed (out of scope — only the OWNER pushes); verified via --dry-run only. Intentionally did not touch `.github/workflows/release.yml` or `project.yml`.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 5m 11s | 290.14k (266.75k) | 8.43k | — | — | review |

total: 1 attempt · 5m 11s · 290.14k in (266.75k cached) / 8.43k out · $0.00 · 1 without cost (accepted 2026-08-31T16:44:09Z)
