---
id: T-0001
title: Read the Claude Keychain credential through the security CLI, never the framework API
priority: P0
complexity: C2
depends_on: []
scope: [QuotaKit/Sources/QuotaKit/Support/Keychain.swift, QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift]
---

## Goal
`quotactl --live` cannot read the Claude token today. `Keychain.genericPassword`
uses `SecItemCopyMatching`, and for a Keychain item another application created
that call is refused for an unsigned SwiftPM binary — it returns
`errSecInteractionNotAllowed` / `errSecAuthFailed`, so the live Claude source
fails 100% of the time with
`Keychain access to 'Claude Code-credentials' was denied`. When it is *not*
refused outright it can instead raise the system "allow access?" dialog, which
is worse: an interactive prompt in a tool meant to print a number and exit.

The `security` command-line tool reads the same item silently. Verified by the
PM on this machine:

```
$ security find-generic-password -s "Claude Code-credentials" -w
<the JSON blob>          # exit 0, no prompt
```

**Decision (product owner, this session): the `security` CLI is the only path
we use.** No Keychain prompts, ever. The framework strategy stays in the file
as an injectable option so a future signed, entitled app bundle can opt into
it, but nothing selects it by default.

## Context
- File to change: `QuotaKit/Sources/QuotaKit/Support/Keychain.swift` (34 lines).
- The single caller is `ClaudeLiveSource.init`'s default `credentialsProvider`
  in `Providers/ClaudeSources.swift`. **Do not change that file** — this ticket
  keeps `Keychain.genericPassword(service:)` working with its current signature.
- `QuotaError` cases and their meanings are in `Providers/QuotaSource.swift`.
  `QuotaError.reportingPriority` (internal) already ranks failures by how
  actionable they are; reuse it, do not reinvent it.
- Follow the injection convention in AGENTS.md: the seam is a parameter
  default, so tests never touch the real Keychain.

## Design — implement exactly this interface
Invent nothing beyond it; if it cannot be built as specified, stop and use the
BLOCKED protocol rather than improvising a different shape.

```swift
/// One strategy for reading a generic-password item. Injectable so tests
/// never touch the real Keychain.
public struct KeychainLookup: Sendable {
    public let name: String                             // "security" / "SecItem"
    public let read: @Sendable (String) throws -> Data   // service name -> item data
    public init(name: String, read: @escaping @Sendable (String) throws -> Data)
}

public enum Keychain {
    /// The `security` CLI, and only it. `SecItemCopyMatching` is deliberately
    /// not in this list: for an item another app owns it is either refused
    /// outright or answered with an interactive "allow access?" dialog, and a
    /// command-line tool must never block on a GUI prompt.
    public static let defaultLookups: [KeychainLookup] = [.securityCLI]

    /// Shells out to `/usr/bin/security`. The default strategy.
    public static let securityCLI: KeychainLookup

    /// The framework API. Not used by default — see `defaultLookups`. Kept as
    /// an opt-in for a signed app bundle that holds the item's ACL.
    public static let secItem: KeychainLookup   // existing SecItemCopyMatching logic, moved here

    public static func genericPassword(
        service: String,
        lookups: [KeychainLookup] = defaultLookups
    ) throws -> Data
}
```

`genericPassword` semantics:
- Try each lookup in order; return the first success.
- Never invoke a later lookup once an earlier one succeeded.
- If all fail, throw the failure with the **highest `QuotaError.reportingPriority`**,
  ties broken by lookup order (earlier wins). A non-`QuotaError` counts as
  priority 2, matching `HybridProvider.mostActionableMessage`.
- An empty `lookups` array throws `QuotaError.notConfigured`.

(The list-of-strategies shape is kept even though the default has one entry:
it is what makes the whole thing testable without a Keychain, and it is how the
app will opt into `secItem` later.)

`securityCLI` semantics:
- Run `/usr/bin/security` by **absolute path** (never resolve via `PATH`), with
  arguments `["find-generic-password", "-s", service, "-w"]`.
- Read stdout to end **before** `waitUntilExit()` — the blob is ~14 KB and
  reading after waiting can deadlock on a full pipe.
- Trim trailing newline/whitespace from stdout, then UTF-8-encode to `Data`.
- Map the outcome:
  | exit | throw |
  |---|---|
  | 0, non-empty output | *(success)* |
  | 0, empty output | `.malformed("Keychain item '<service>' held no data")` |
  | 44 | `.notConfigured("No Keychain item named '<service>'")` |
  | 51 or 128 | `.unauthorized("Keychain access to '<service>' was denied")` |
  | anything else | `.transport("`security` failed for '<service>' (exit <n>)")` |
- If the process cannot be launched at all, throw `.transport`.
- **Never** write stdout to a log, an error message, or stderr passthrough — it
  is the access token. Error messages may name the service and the exit code
  and nothing else.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] A test asserts `Keychain.defaultLookups.map(\.name) == ["security"]` —
      that the framework API is not consulted by default is the point of the
      ticket, so it gets its own assertion
- [ ] New tests, all using **injected** `KeychainLookup`s (no real Keychain, no
      `Process`, no network — see AGENTS.md gotcha 3), covering:
      - the first lookup succeeding means the second is never invoked
        (assert with a flag the stub sets)
      - the first failing and the second succeeding returns the second's data
      - all failing throws the most *actionable* error, not merely the last:
        given `[.unauthorized, .notConfigured]` the thrown error is the
        `.unauthorized`; given `[.notConfigured, .unauthorized]` it is still
        the `.unauthorized`
      - an empty `lookups` array throws `.notConfigured`
- [ ] Every new public symbol has a doc comment; the comment on `defaultLookups`
      states *why* the CLI is the only default (refusal + GUI prompt, above)
- [ ] `Keychain.genericPassword(service:)` still compiles for the existing
      caller in `ClaudeSources.swift` with no change to that file
- [ ] No files changed outside `scope`

## Out of scope
- Editing `ClaudeSources.swift`, `main.swift`, or anything under `App/`,
  `Widget/`, or `QuotaKit/Sources/QuotaKit/UI/`.
- Deleting the `secItem` strategy — keep it, just do not default to it.
- Refreshing an expired OAuth token, or writing to the Keychain.
- Any test that shells out to the real `security` binary. It will not work
  under the worker sandbox and will be rejected.

## Worker report

> claimed_by: codex (pid 94579) at 2026-08-29T16:28:05Z

### Files changed
- `QuotaKit/Sources/QuotaKit/Support/Keychain.swift` — added injectable lookup strategies, made `/usr/bin/security` the sole default, retained `SecItemCopyMatching` as opt-in, and selected the most actionable failure.
- `QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift` — added injected lookup tests for the default, short-circuiting, fallback success, actionable-error ordering, and an empty lookup list.

### Commands run
- `bash .tigerteam/scripts/run-tests.sh` — `exit: 0`; `Test run with 42 tests in 0 suites passed after 0.006 seconds.` (log `.tigerteam/logs/tests/20260829-164002-5956.log`).
- `git status --short && git diff --check -- QuotaKit/Sources/QuotaKit/Support/Keychain.swift QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift && git diff --stat -- QuotaKit/Sources/QuotaKit/Support/Keychain.swift QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift && git diff -- QuotaKit/Sources/QuotaKit/Support/Keychain.swift QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift` — only the two scoped files were modified; no whitespace errors.
- `git add QuotaKit/Sources/QuotaKit/Support/Keychain.swift QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift && git commit -m "[T-0001] Read Claude credentials through security CLI"` — committed successfully as `2d15a24`.
- `git status --short && git rev-parse HEAD && git branch --show-current && git show --stat --oneline --summary HEAD` — clean tree; confirmed commit `2d15a242b0a162f66efebc0b96e080313466a204` on `tigerteam/T-0001` contains only the two scoped files.

Prior blocked-attempt commands, retained for history:
- `bash .tigerteam/scripts/run-tests.sh` — exit 2: `run-tests: TIGERTEAM_TEST_CMD not set (no test_cmd in tigerteam.toml either)`.
- `TIGERTEAM_TEST_CMD='swift test --package-path QuotaKit' bash .tigerteam/scripts/run-tests.sh` — exit 1: SwiftPM manifest compilation was blocked by `sandbox-exec: sandbox_apply: Operation not permitted` (log `.tigerteam/logs/tests/20260829-163207-2215.log`).
- `TIGERTEAM_TEST_CMD='swift test --disable-sandbox --package-path QuotaKit' bash .tigerteam/scripts/run-tests.sh` — exit 1 after compiling `Keychain.swift`: SwiftPM could not apply the built `quotactl` executable (`Operation not permitted`; log `.tigerteam/logs/tests/20260829-163216-2283.log`).
- `TIGERTEAM_TEST_CMD='swift build --disable-sandbox --package-path QuotaKit --target QuotaKitTests' bash .tigerteam/scripts/run-tests.sh` — `exit: 0`; `Build of target: 'QuotaKitTests' complete!` (final log `.tigerteam/logs/tests/20260829-163320-2627.log`). This compiled the production change and all new tests but did not execute tests.
- `git add QuotaKit/Sources/QuotaKit/Support/Keychain.swift QuotaKit/Tests/QuotaKitTests/QuotaKitTests.swift && git commit -m "[T-0001] Read Claude credentials through security CLI"` — failed before staging: `fatal: Invalid path '/Users/d/b': Operation not permitted`.

### Documentation
- Added doc comments for `KeychainLookup`, both public properties, its initializer, both strategies, `defaultLookups`, and the updated `genericPassword` interface.
- The `defaultLookups` comment documents both framework refusal and the possible GUI prompt as reasons the CLI is the only default.

### Commit
- `2d15a242b0a162f66efebc0b96e080313466a204` on branch `tigerteam/T-0001`.

### Reviewer notes
- Full tests now pass through the required wrapper, including compilation of the unchanged `ClaudeLiveSource` caller and all five injected lookup tests.
- The dedicated default assertion is `Keychain.defaultLookups.map(\.name) == ["security"]`; actionable-error tests cover both lookup orderings.
- The prior infrastructure blockers were resolved by the PM. No out-of-scope source files were edited, and the final worktree is clean.

## Questions
The worker environment blocks both required completion operations: SwiftPM cannot apply/execute the built test products, and Git cannot stat the worktree's parent path. The options are (1) rerun this ticket in a worker with a configured `TIGERTEAM_TEST_CMD` and access to `/Users/d/b`, or (2) have the PM execute tests and commit the two scoped files outside this worker. Recommendation: rerun in a corrected worker environment so the prescribed wrapper and scoped commit can both complete normally.

> handoff: blocked

## Answers

## PM answer (2026-08-29)

Your diagnosis was correct on all three counts, and blocking was the right
call — none of it was fixable from inside the worker. All three are now fixed
in the environment. **Your code was never the problem.**

### What was wrong and what changed

1. **`TIGERTEAM_TEST_CMD not set`** — the supervisor process was started
   *before* `test_cmd` was added to `tigerteam.toml`, so it injected nothing.
   The supervisor has been restarted. `bash .tigerteam/scripts/run-tests.sh`
   now resolves the command on its own. Use it plain, with no environment
   prefix — do not set `TIGERTEAM_TEST_CMD` yourself.

2. **`sandbox_apply: Operation not permitted`** — real, and it is why
   `test_cmd` carries `--disable-sandbox`: SwiftPM evaluates `Package.swift`
   in its own nested `sandbox-exec`, and macOS refuses to nest Seatbelt
   sandboxes. The configured command already includes the flag, so the
   wrapper handles this for you.

3. **`quotactl: Operation not permitted` and `git fatal: Invalid path
   '/Users/d/b'`** — one shared root cause, and a genuine bug in the sandbox
   profile that you surfaced. The profile denied all of `$HOME` and re-allowed
   only the project root, never the intermediate directories between them.
   Every tool that resolves a path stats each component on the way down, so
   `git` could not stat the repo's parent and `codesign` could not sign the
   built executable — which on macOS fails any `swift build` that produces one.
   `worker.sb` now allows path-component *metadata* everywhere (contents stay
   denied). Committed as `f7d5b10`.

### Verified by the PM, in your worktree, under the fixed sandbox
`swift test --disable-sandbox` → **42 tests passed** (baseline was 37, so your
five new tests are present and green). `git status` and `codesign` both work
there now.

### What to do now
Your edits to `Keychain.swift` and `QuotaKitTests.swift` are still in the
worktree, uncommitted. Do not redo them — pick up where you left off:

1. Run `bash .tigerteam/scripts/run-tests.sh` and confirm exit 0.
2. Re-check the acceptance criteria against what you wrote, in particular the
   `defaultLookups.map(\.name) == ["security"]` assertion and the
   most-actionable-error ordering tests.
3. Commit **only** the two files in `scope`, then fill in the Worker report
   with the run-tests output and hand off to review.

If anything still fails on infrastructure rather than on the code, block again
with the exact error — that was the correct behaviour and it is what got these
three bugs found.


> claimed_by: codex (pid 5605) at 2026-08-29T16:39:34Z

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | codex | — | 5m 59s | 674.16k (621.06k) | 15.01k | — | — | blocked |
| 2 | codex | — | 1m 45s | 359.04k (320.77k) | 4.27k | — | — | review |

total: 2 attempts · 7m 44s · 1.03M in (941.82k cached) / 19.28k out · $0.00 · 2 without cost (accepted 2026-08-29T16:44:25Z)
