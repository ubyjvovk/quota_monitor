---
id: T-0036
title: Menu bar icon bars in snapshot order (same order as the panel), not ranked by usage
priority: P1
complexity: C1
assignee: opus
capability: [macbuild]
depends_on: []
scope: [App/MenuBarIcon.swift]
---

## Goal
The status-bar icon stacks one bar per provider, currently in
`rankedProviders` order (most constrained on top). The panel below it lists
providers in snapshot order (the console's fixed registry order), so the
bars and the rows disagree — the user reads bar 1 as Claude and it is not.
Draw the bars in snapshot order, top to bottom, exactly matching the panel.

## Context
- `App/MenuBarIcon.swift` — `image(for:now:)`; the only line that decides
  order is `let providers = snapshot.rankedProviders(asOf: now)`.
- The panel renders `ConsoleTable.render(engine.snapshot.providers, asOf:)`
  (`App/QuotaPanelView.swift`) — snapshot order. `QuotaFormat.menuBarTitle`
  (the icon's accessibility description) already iterates `snapshot.providers`.
- Your lane cannot run `xcodebuild`; the PM builds and re-renders
  `docs/menubar.png` at review.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] `grep -rn "rankedProviders" App/` prints nothing; `grep -n "snapshot.providers" App/MenuBarIcon.swift` matches
- [ ] The comment inside the draw loop that says the top bar is the most constrained provider is rewritten to say the bars follow the panel's (snapshot) order, top to bottom
- [ ] Doc comment on `MenuBarIcon` updated to say the same (one clause); no other docs
- [ ] No files changed outside scope

## Out of scope
Colours, sizes, the widget, `QuotaKit/`, the panel, any re-ranking option.

## Worker report

> claimed_by: opus-1 (pid 83690) at 2026-08-30T17:15:51Z

**Files changed**
- `App/MenuBarIcon.swift` — `image(for:now:)` now stacks bars from
  `snapshot.providers` (snapshot/registry order) instead of
  `snapshot.rankedProviders(asOf:)`, so bar N is the panel's row N; the
  draw-loop comment and the `MenuBarIcon` doc comment were rewritten to say
  the bars follow the panel's (snapshot) order top to bottom.

**Commands run**
- `bash .tigerteam/scripts/run-tests.sh` → `### exit 0`
  - `✔ Test run with 79 tests in 2 suites passed after 0.007 seconds.`
  - `### go test (core)` — all packages `ok` (`cmd/quotamon`, `internal/cache`,
    `config`, `discover`, `format`, `hybrid`, `jsonx`, `providers/{claude,codex,
    deepinfra,grok,kimi}`, `registry`, `snapshot`, `source`);
    `internal/fixtures` has no test files.
- `grep -rn "rankedProviders" App/` → no output (criterion met).
- `grep -n "snapshot.providers" App/MenuBarIcon.swift` →
  `15:        let providers = snapshot.providers` (criterion met).
- `git status --porcelain` → `M App/MenuBarIcon.swift` only; nothing outside scope.

**Doc changes**
- Only the two in-file comments the ticket names (type doc comment + draw-loop
  comment). No doc files touched, per the ticket.

**Commit**
- `fc6ba88` on branch `tigerteam/T-0036`.

**For the reviewer**
- Not verified by me: the app build and the `docs/menubar.png` re-render. This
  lane cannot run `xcodebuild` (per the ticket's Context), so the change is
  compile-checked only indirectly — `App/` is not part of the SwiftPM package
  that `run-tests.sh` builds. The change is a one-line substitution of an
  existing `[ProviderSnapshot]` property for a method returning the same type,
  so a build break is unlikely, but the PM should confirm at review.
- Behaviour note, not a defect: `rankedProviders` also sorted providers that
  have no current reading to the bottom. Bars now sit in fixed registry order,
  so an empty track can appear above a filled one — that is exactly what the
  panel does, and what the ticket asks for.
- Out of scope, untouched: `QuotaKit/Sources/QuotaKit/UI/ConsoleWidgetView.swift:54`
  still uses `rankedProviders(asOf:)`, so the widget keeps usage-ranked order and
  now disagrees with both the panel and the menu bar icon. Worth a follow-up
  ticket if the same consistency argument applies there.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | opus-1 | claude-opus-5 | 1m 32s | 350.36k (350.33k) | 4.12k | $0.51 | engine | review |

total: 1 attempt · 1m 32s · 350.36k in (350.33k cached) / 4.12k out · $0.51 (accepted 2026-08-30T17:18:04Z)
