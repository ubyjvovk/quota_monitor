---
id: T-0024
title: setup must not pin newly-supported providers off (no config entries for unsupported providers)
priority: P2
complexity: C1
depends_on: []
scope: [core/internal/config/config.go, core/internal/config/config_test.go, core/cmd/quotamon/setup.go, core/cmd/quotamon/setup_test.go]
---

## Goal
`quotamon setup` wrote `"kimi": {"enabled": false}` while Kimi was still
unsupported (from `config.Default()`), and `setup --yes` treats an on-disk
value as the user's choice. Result on the reference machine: after Kimi
became supported, `setup --yes` reported `✓ Kimi … found` yet left it off,
and the table never showed it. Non-interactive setup must be able to adopt
a newly supported, found provider.

## Design — implement exactly this
- `config.Default()` returns an **empty** providers map (no entries).
- `setup` writes entries **only** for supported providers, and only those the
  user decided on (interactive: every supported provider is asked, so all
  get entries; `--yes`: entries for found providers = enabled, and for
  not-found providers = disabled). Unsupported providers never get an entry.
- `--yes` rule: **enable every found supported provider; keep existing
  keys; not-found providers get `enabled:false`.** Print
  `Enabled every provider that was found; run without --yes to choose.`
  Users who want a found provider off run interactive setup or edit the file.
- Loading a config that has an entry for an unknown/unsupported id must not
  fail (ignore it); the registry already omits unknown ids — verify.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] Test: config on disk `{kimi:{enabled:false}}`, findings say Kimi found+supported → `setup --yes` writes `kimi.enabled == true` and prints the "run without --yes to choose" line
- [ ] Test: `--yes` with DeepInfra key already in the file keeps the key (existing test still passes)
- [ ] Test: `Default()` has no provider entries; interactive setup with an unsupported finding writes no entry for it
- [ ] `core/README.md` Configuration → First run: one sentence on the `--yes` rule (add to scope in your report)
- [ ] No files changed outside scope (+ `core/README.md`)

## Out of scope
Discovery probes, provider packages, table rendering.

## Worker report

### Files changed (all in scope + core/README.md)
- `core/internal/config/config.go` — `Default()` now returns an **empty**
  providers map instead of pinning every known provider (incl. unsupported
  Kimi) off; comments explain why.
- `core/internal/config/config_test.go` — round-trip test now sets `claude`
  explicitly (Default no longer provides it); added
  `TestDefaultHasNoProviderEntries` and `TestLoadIgnoresAnUnknownProviderEntry`
  (verifies the "unknown id must not fail Load" criterion).
- `core/cmd/quotamon/setup.go` — `--yes` rule becomes
  `enabled = finding.Found || existingEntry.Enabled`: it adopts every found
  supported provider even over a stale `off` entry (the Kimi bug), keeps
  manually enabled providers with stored keys, and prints
  `Enabled every provider that was found; run without --yes to choose.`
- `core/cmd/quotamon/setup_test.go` — added
  `TestSetupYesAdoptsANewlySupportedFoundProviderWhoseEntryWasOff` (stale
  `kimi:false` on disk + found+supported → `kimi.enabled==true` + the note
  line), and asserted the existing interactive test writes **no** entry for
  the unsupported Kimi finding.
- `core/README.md` — Configuration → First run: one sentence on the `--yes`
  rule (enables every found supported provider, even one pinned off before it
  was supported; to leave a found provider off run without `--yes` or edit the
  file).

### Commands run
`bash .tigerteam/scripts/run-tests.sh` — exit 0. Summary lines:
```
### go test (core)
ok quotamon/cmd/quotamon 0.526s
ok quotamon/internal/config 0.283s
... (14 packages ok) ...
### exit 0
```
Swift: `Test run with 62 tests in 1 suite passed`.
Also hand-verified end-to-end: seeded a temp config `{kimi:{enabled:false}}`
and ran `QUOTA_MONITOR_DIR=<tmp> go run ./cmd/quotamon setup --yes` — Kimi
overridden to `enabled:true`, the "run without --yes to choose" line printed,
exit 0. (Temp dir only; real config untouched.)

### Acceptance criteria walk
- [x] `run-tests.sh` exits 0 (final run, both suites green).
- [x] Test `TestSetupYesAdoptsANewlySupportedFoundProviderWhoseEntryWasOff`
  covers stale `kimi:false` + found/supported → `true` + note line.
- [x] Existing `TestSetupYesRewritesWithoutReadingStdinAndKeepsThePastedKey`
  still passes (DeepInfra key kept; the `--yes` rule `found || existingEnabled`
  preserves the manually enabled keyed provider).
- [x] `TestDefaultHasNoProviderEntries` + interactive test now asserts no entry
  for the unsupported Kimi finding; `TestLoadIgnoresAnUnknownProviderEntry`
  confirms an unknown id loads without failing.
- [x] `core/README.md` First run sentence added.
- [x] `git status` shows only the 4 scope files + `core/README.md`.

### Doc changes
- `core/README.md` First run: `--yes` sentence (above).

### Commit
- `1c15f96` on branch `tigerteam/T-0024` — "[T-0024] setup must not pin
  newly-supported providers off".

### For the reviewer
- Design wording "not-found providers get enabled:false" is satisfied for
  fresh/off entries; a not-found provider that is **already enabled with a
  stored key** stays enabled to satisfy the explicit acceptance criterion
  "DeepInfra key already in the file keeps the key (existing test still
  passes)" and "keep existing keys". Rule implemented is exactly
  `enabled = found || existingEntry.Enabled`.
- Out of scope, noticed only: `config.Load` tolerates (keeps) an unknown id
  entry rather than stripping it; per design it is "ignored" — setup does not
  purge stale unsupported entries, so manual choices survive a re-run.

### Re-claim (ds-1) verification
Prior attempt was flagged INCOMPLETE ("engine exit 0, no handoff") despite the
work being committed on the ticket branch. Re-claim confirmed the committed
state is complete and re-ran the acceptance-criteria tests through the wrapper:
`bash .tigerteam/scripts/run-tests.sh` exits 0 (Swift `62 tests passed`; Go all
packages `ok`). Targeted re-runs, all PASS:
`TestDefaultHasNoProviderEntries`, `TestLoadIgnoresAnUnknownProviderEntry`,
`TestSetupYesRewritesWithoutReadingStdinAndKeepsThePastedKey`, and
`TestSetupYesAdoptsANewlySupportedFoundProviderWhoseEntryWasOff`. Scope check:
`git status` clean; commit `1c15f96` covers only the 4 scope files +
`core/README.md`.

> handoff: review
> INCOMPLETE attempt by ds-1 at 2026-08-29T21:44:43Z (engine exit 0, no handoff) — see /Users/d/b/quota_monitor/.tigerteam/logs/workers/ds-1.log

> claimed_by: ds-1 (pid 65930) at 2026-08-29T21:44:43Z

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 6m 14s | 1.3M (1.24M) | 21.59k | — | — | todo |
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 3m 2s | 291.24k (273.15k) | 6.65k | — | — | review |

total: 2 attempts · 9m 17s · 1.59M in (1.52M cached) / 28.25k out · $0.00 · 2 without cost (accepted 2026-08-29T21:49:12Z)
