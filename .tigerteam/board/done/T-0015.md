---
id: T-0015
title: quotamon with no subcommand prints a human-readable table of every provider
priority: P1
complexity: C2
depends_on: []
scope: [core/cmd/quotamon/main.go, core/cmd/quotamon/main_test.go, core/cmd/quotamon/table.go, core/cmd/quotamon/table_test.go, core/internal/format/, core/internal/snapshot/]
---

## Goal
`quotamon` typed bare at a prompt should show all quotas at once, the way
`swift run quotactl` does today. `snapshot` (JSON) and `waybar` stay as they
are; this adds the default human view and a `--json` alias.

## Context
- Port the layout of `QuotaKit/Sources/QuotaKit/Support/ConsoleReport.swift`
  (`render`, `windowLine`, `creditsLine`, `originTag`) — read it, it is short
  and every rule in it was learned from a real bug.
- `renderWaybar`/`providerTooltip` in `core/cmd/quotamon/waybar.go` already
  compute most of the strings; reuse `format.Percent/Countdown/Age`.
- Providers today: claude, codex, grok; T-0013 (DeepInfra) runs in parallel
  and touches ONLY `core/cmd/quotamon/waybar.go` + its own package — so put the
  renderer in NEW files `table.go` / `table_test.go` and limit `main.go` edits
  to dispatch. Do not edit `waybar.go`; if you need `providerShortName`, it is
  not needed for the table (use `DisplayName`).

## Design — implement exactly this
```
quotamon [--no-live]          → table below, exit 0 if any provider has a window, else 1
quotamon --json [--no-live]   → identical to `quotamon snapshot`
```
Table, one block per provider in registry order, 2-space indent:
```
<DisplayName padded 15> <plan or —, padded 6> ... <origin word> · <age>
  <label padded 14> <percent right-aligned 6, "—" when no reading>  resets in <countdown> | window reset | (nothing when no resetsAt)
  credits        <line per creditsLine rules>          (omitted when creditsLine is nil)
  !  <status message>                                  (only when status.state != ok)
```
`creditsLine` rules (from Swift): `unlimited` → "unlimited"; enabled with
balance → "<balance> remaining"; !enabled with a non-empty balance →
"<balance> (not enabled)"; !enabled and no balance → omit the line.
For DeepInfra the balance is already the "$7.75 this month" string, and it is
Enabled+Unlimited when no limit exists → render "$7.75 this month" (special-case:
when Unlimited and Balance != nil, print the balance verbatim instead of "unlimited").
Windows sorted most-constrained first (reading > no reading, then percent desc,
then kind rank) — add `snapshot.Provider.SortedWindows(now)` if it does not exist
(`core/internal/snapshot/` is in scope for that one function only).
Origin word: live→"live", local→"cached", unavailable→"unavailable".

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] Pure render function tested with a fixed `now` on a hand-built snapshot covering: a reading, a rolled-over window (`—`), a `(not enabled)` credit, a DeepInfra-style `$7.75 this month` credit, and an unavailable provider with a status line; expected output asserted as a literal multi-line string
- [ ] `cd core && go run ./cmd/quotamon --no-live` exits 0 or 1 without panic; `go run ./cmd/quotamon --json --no-live | python3 -m json.tool` succeeds
- [ ] `core/README.md` usage section shows the bare invocation first with a sample table
- [ ] No files changed outside scope

## Out of scope
Colour/ANSI, history/pace, any change to `snapshot`/`waybar` output.

## Worker report

> claimed_by: codex-1 (pid 65930) at 2026-08-29T20:22:44Z

### Files changed
- `core/cmd/quotamon/table.go` — added the pure human table renderer, including window, credit, origin, age, and status formatting.
- `core/cmd/quotamon/table_test.go` — asserted the complete fixed-time output as a literal multiline string covering every required rendering case.
- `core/cmd/quotamon/main.go` — dispatched bare/`--no-live` invocations to the table and `--json` to the existing snapshot path with shared window-based exit status.
- `core/cmd/quotamon/main_test.go` — covered default table dispatch, exit status, JSON alias parity, and invalid argument forms.
- `core/internal/snapshot/snapshot.go` — added the documented, non-mutating `Provider.SortedWindows(now)` ordering helper.
- `core/internal/snapshot/snapshot_test.go` — covered reading/percentage/kind/rollover ordering and verified the original slice is not mutated.
- `core/README.md` — documented the bare invocation first, its sample table, the JSON alias, and table exit codes as explicitly required by acceptance.

### Commands run
- Baseline `bash .tigerteam/scripts/run-tests.sh` — exit 0; log `.tigerteam/logs/tests/20260829-202316-72001.log`.
  Summary: `ok quotamon/cmd/quotamon 0.915s`; `ok quotamon/internal/format 0.257s`; `ok quotamon/internal/hybrid 0.466s`; `ok quotamon/internal/jsonx 0.671s`; `ok quotamon/internal/providers/claude 1.633s`; `ok quotamon/internal/providers/codex 2.494s`; `ok quotamon/internal/providers/grok 2.083s`; `ok quotamon/internal/registry 1.848s`; `ok quotamon/internal/snapshot 2.290s`; `ok quotamon/internal/source 1.366s`; `### exit 0`.
- Final `bash .tigerteam/scripts/run-tests.sh` — exit 0; log `.tigerteam/logs/tests/20260829-202551-73138.log`.
  Summary: `ok quotamon/cmd/quotamon 0.700s`; `ok quotamon/internal/format (cached)`; `ok quotamon/internal/hybrid 0.333s`; `ok quotamon/internal/jsonx (cached)`; `ok quotamon/internal/providers/claude 0.937s`; `ok quotamon/internal/providers/codex 2.143s`; `ok quotamon/internal/providers/grok 1.632s`; `ok quotamon/internal/registry 1.833s`; `ok quotamon/internal/snapshot 2.054s`; `ok quotamon/internal/source (cached)`; `### exit 0`.
- `cd core && go run ./cmd/quotamon --no-live` — exit 0, rendered all configured providers without panic.
- `cd core && go run ./cmd/quotamon --json --no-live | python3 -m json.tool` with `pipefail` — exit 0 and parsed successfully.
- `gofmt -w` on the six changed Go files; `git diff --check` — clean.

### Documentation
- Updated `core/README.md` Usage so the bare command is first, followed by a representative table and the `--json` alias/exit behavior.
- Added the public `SortedWindows` Go doc comment.

### Commit
- `72bd865` on `tigerteam/T-0015` — `[T-0015] quotamon with no subcommand prints a human-readable table of every provider`.

### Reviewer notes
- The renderer applies the DeepInfra rule generically: any unlimited credit with a non-nil balance prints that provider-formatted balance verbatim.
- `core/README.md` is absent from the frontmatter scope list but is explicitly required by an acceptance criterion; it is the only changed path outside that literal list.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | codex-1 | gpt-5.6-sol | 5m 20s | 680.05k (622.59k) | 13.84k | — | — | review |

total: 1 attempt · 5m 20s · 680.05k in (622.59k cached) / 13.84k out · $0.00 · 1 without cost (accepted 2026-08-29T20:30:34Z)
