---
id: T-0019
title: quotamon setup — discover providers, confirm with the user, write the config
priority: P1
complexity: C2
depends_on: [T-0018]
scope: [core/cmd/quotamon/setup.go, core/cmd/quotamon/setup_test.go, core/cmd/quotamon/main.go, core/cmd/quotamon/main_test.go]
---

## Goal
The user-friendly first run. `quotamon setup` auto-discovers every provider
quotamon knows how to read, shows the findings, lets the user enable/disable
each and add what discovery could not find (an API key), and writes the
mandatory config file (T-0018). Re-runnable: pre-fills from the existing file.

## Context
- `discover.All()` and `config` from T-0018. Findings include Kimi as
  found-but-unsupported so the user is not left wondering.
- Interaction must be testable: read prompts from an injected `io.Reader`,
  write to an injected `io.Writer`; no TTY tricks, no third-party libs.

## Design — implement exactly this
```
quotamon setup [--yes]
```
Flow (plain text, one screen):
1. Print `Looking for providers…` then a table, one row per finding:
   `✓ Claude      Keychain item present`
   `✓ ChatGPT     ~/.codex/auth.json`
   `✗ Grok        not signed in — run `grok login``
   `✗ DeepInfra   no API key`
   `· Kimi        credentials found, but Kimi exposes no quota API yet`
2. For each **supported** provider, ask `Enable <name>? [Y/n]` — default Y
   when found, n when not found (existing config value wins as the default when
   re-running). For a provider with `NeedsKey` that is enabled and has no key,
   prompt `DeepInfra API key (input hidden is not available; paste and press Enter):`
   — accept empty to skip and leave it disabled.
3. Ask `Add anything else manually? [y/N]` — on y, list the known provider ids
   again and let the user pick one to enable (this exists so a provider that
   discovery missed, e.g. codex on an unusual path, can still be turned on;
   there is nothing to "add" beyond the known set — say so in the prompt:
   `quotamon can read: claude, codex, grok, deepinfra. Enter an id, or blank:`).
4. Write the config; print `Wrote <path> (mode 0600). Run: quotamon`.
`--yes`: no prompts — enable exactly what was found, keys from env only.
Also implement `quotamon providers`: the same table plus an `enabled` column
from the config (no prompts, exit 0; exit 3 with the setup hint when no config).

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] setup test with scripted stdin (`"\ny\nabc123\nn\n"`-style) and a stubbed finding set in a temp `QUOTA_MONITOR_DIR`: writes config.json 0600 with claude enabled, grok disabled, deepinfra enabled with `api_key:"abc123"`; re-running with `--yes` keeps the key
- [ ] `--yes` writes without reading stdin at all (stdin is `iotest.ErrReader` in the test)
- [ ] `providers` prints the table with the enabled column; exit 3 without a config
- [ ] `core/README.md` "Configuration" section gains a "First run" subsection showing the setup transcript; root-README quick start is T-0016's job — if T-0016 already landed, update its quick start to start with `quotamon setup` (add `README.md` to scope in your report)
- [ ] No files changed outside scope

## Out of scope
Hidden password input, colours, network probes, editing providers' packages.

## Worker report

> claimed_by: ds-1 (pid 65930) at 2026-08-29T20:52:56Z

The `setup` and `providers` stubs in `runWithFactory` are now real implementations.

**Files changed**
- `core/cmd/quotamon/setup.go` (new) — `runSetup` (discover → render findings → per-provider Y/n → key prompt for enabled NeedsKey providers → manual add → Save → `Wrote <path> (mode 0600). Run: quotamon`) and `runProviders` (table + `on`/`off` column; exit 3 with the setup hint when config is missing). Findings table renderer + prompt/yes-no helpers live here too.
- `core/cmd/quotamon/setup_test.go` (new) — scripted-stdin + stubbed-findings tests for the interactive flow, `--yes` (proves it never reads stdin via `noReadReader`), `--yes` re-run keeping the pasted key, the unsupported-Kimi row, and `providers` with/without config.
- `core/cmd/quotamon/main.go` — threaded `stdin io.Reader` into `run`/`runWithFactory`; routed `setup`/`providers` before the config load; `parseArguments` now returns a `yes` flag and accepts `setup --yes`; dropped the not-implemented stub; updated usage text.
- `core/cmd/quotamon/main_test.go` — passed `strings.NewReader("")` as stdin at every call site; removed the obsolete `TestSetupAndProvidersStubsPrintNotImplemented`.
- `core/README.md` (**added to scope per the ticket**) — Configuration section gained a `### First run` subsection with the setup transcript, the Y/n-default rule, the key prompt, manual add, `--yes`, and `providers` behaviour.

**Commands run**
- `bash .tigerteam/scripts/run-tests.sh` → exit 0. Swift: 62 tests in 1 suite passed. Go: `cmd/quotamon`, `config`, `discover`, `format`, `hybrid`, `jsonx`, `claude`, `codex`, `deepinfra`, `grok`, `registry`, `snapshot`, `source` all `ok`. (`go vet ./...` runs inside the wrapper.)
- Hand-checks: `QUOTA_MONITOR_DIR=$(mktemp -d) go run ./cmd/quotamon setup --yes` writes config 0600, enables what discovery found, and leaves the DeepInfra key in env (not the file); scripted-stdin interactive run stores a pasted key; `providers` prints the table with `on`/`off` and exits 0, and exits 3 with the setup hint when no config exists.

**Doc changes** — `core/README.md` `### First run` under Configuration (see above).

**Commit** — `4ed89967f9499717effaf8b0bfc662e9f7e6965d` on branch `tigerteam/T-0019`.

**Reviewer should look at** — the `--yes` semantics: it enables `found || already-enabled-on-disk`, which is what makes a re-run keep a pasted key while still leaving a never-configured, not-found provider off. The "T-0016 quick start" doc criterion is not applicable here: root-README quick start is T-0016's job and T-0016 has not landed (confirmed via git log; only board commits reference it). Out of scope, not touched: hidden password input, colours, network probes, and provider package internals.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-1 | deepseek-ai/DeepSeek-V4-Flash-0731 | 11m 52s | 2.36M (2.15M) | 39.86k | — | — | review |

total: 1 attempt · 11m 52s · 2.36M in (2.15M cached) / 39.86k out · $0.00 · 1 without cost (accepted 2026-08-29T21:06:24Z)
