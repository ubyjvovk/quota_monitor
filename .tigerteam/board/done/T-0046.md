---
id: T-0046
title: Waybar output matches the table: sorted windows, cached label, credits always, clamped percentage
priority: P2
complexity: C2
depends_on: []
scope: [core/cmd/quotamon/waybar.go, core/cmd/quotamon/waybar_test.go]
---

## Goal
The waybar tooltip diverges from the console table it claims to mirror
(findings opus F11, codex F13/F14, ds F4/F9 in
`.tigerteam/reviews/2026-08-31-*.md`).

## Design — implement exactly this
- Tooltip iterates `provider.SortedWindows(now)` (table order).
- Origin rendered via the same mapping as `tableOrigin` (live/cached/
  unavailable) — reuse or mirror it, never the raw string.
- Credit rows via `creditLines(...)` whenever `provider.Credits != nil`
  (drop the `len(Windows)==0` guard); table.go's creditLines is in package
  main — call it directly.
- `Percentage` field clamped to 0..100 (`int(math.Round(min(100, max(0, p))))`);
  Text/tooltip keep the unclamped value.
- Tests: tooltip ordering (session 90 after weekly 40 in payload order →
  session first), origin "cached" for local, credits present with windows,
  percentage 104 → 100.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] The four test cases above exist and pass
- [ ] `grep -n "SortedWindows" core/cmd/quotamon/waybar.go` matches
- [ ] No files changed outside scope

## Out of scope
table.go itself, main.go, snapshot model.

## Worker report

> claimed_by: luna (pid 83690) at 2026-08-31T14:54:53Z

- Files changed:
  - `core/cmd/quotamon/waybar.go` — aligned tooltip ordering/origin/credit rendering with the table and clamped the Waybar percentage field.
  - `core/cmd/quotamon/waybar_test.go` — added regression coverage for sorted windows, cached origin, credits with windows, and percentage clamping.
- Commands run:
  - `bash .tigerteam/scripts/run-tests.sh`
    - `cmd:  bash scripts/test-all.sh`
    - `exit: 0`
    - `ok  \tquotamon/cmd/quotamon\t1.789s`
    - `✔ Test run with 83 tests in 2 suites passed after 0.011 seconds.`
    - `### exit 0`
  - `grep -n "SortedWindows" core/cmd/quotamon/waybar.go` → `65: for _, window := range provider.SortedWindows(now) {`
  - `git diff --check` → passed; status showed only the two scoped files before commit.
- Documentation: no documentation files were required; the worker report records the implementation.
- Commit: `af9bbb791d5bcbf0c85c13cfab23f15988e1aa13` on `tigerteam/T-0046`.
- Reviewer notes: `table.go`, `main.go`, and the snapshot model were not changed. No out-of-scope issues were found.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | luna | gpt-5.6-luna | 2m 44s | 809.41k (743.68k) | 5.75k | — | — | review |

total: 1 attempt · 2m 44s · 809.41k in (743.68k cached) / 5.75k out · $0.00 · 1 without cost (accepted 2026-08-31T14:59:46Z)
