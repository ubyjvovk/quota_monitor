---
id: T-0023
title: DeepInfra fetch must not blow the 10 s budget — parallel calls, per-call timeout
priority: P1
complexity: C1
depends_on: []
scope: [core/internal/providers/deepinfra/sources.go, core/internal/providers/deepinfra/deepinfra_test.go]
---

## Goal
DeepInfra intermittently reports `Could not reach DeepInfra payment endpoint …
context deadline exceeded`. `/payment/usage?from=current` alone takes ~4–5 s
on the reference machine and `LiveSource.Fetch` calls `/payment/config` and
then `/payment/usage` **sequentially** inside the CLI's 10 s overall budget,
alongside four other providers. Twice in three runs it lost the race.

## Context
- `core/internal/providers/deepinfra/sources.go` — `Fetch` → `fetchJSON` ×2.
- The CLI budget (10 s, `cmd/quotamon`) is out of scope; fix the provider.

## Design — implement exactly this
- Issue the two GETs **concurrently** (two goroutines, `sync.WaitGroup` or
  `errgroup`-free channel), each with its own 8 s `context.WithTimeout`
  derived from the caller's ctx. Join; if either failed, return that error
  (Unauthorized beats Transport if both fail).
- If `/payment/config` fails but `/payment/usage` succeeded, still return the
  spend reading with `hasLimit=false` and status `NeedsSetup("Spending limit
  unknown — DeepInfra /payment/config: <err>")` — the spend number is the
  valuable part.
- Wrap a deadline error as `source.Transport("DeepInfra is slow to answer
  (>8 s) — will retry next refresh")` instead of the raw `context deadline
  exceeded` text.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0
- [ ] httptest test: usage handler sleeps 300 ms, config sleeps 300 ms → `Fetch` completes in < 500 ms (asserts concurrency)
- [ ] httptest test: config handler returns 500, usage OK → spend reading returned with the NeedsSetup status above
- [ ] httptest test: usage handler sleeps past a 100 ms per-call timeout injected via a `CallTimeout time.Duration` field (default 8 s) → Transport error whose message contains "slow to answer"
- [ ] Doc comment on `Fetch` explains why the calls are parallel
- [ ] No files changed outside scope

## Out of scope
Changing the CLI budget, caching (separate ticket), other providers.

## Worker report

> claimed_by: codex-1 (pid 65930) at 2026-08-29T21:37:19Z

### Files changed
- `core/internal/providers/deepinfra/sources.go` — fetches config and usage concurrently with independent per-call deadlines, preserves spend on config failure, and maps deadline errors to the requested transport message.
- `core/internal/providers/deepinfra/deepinfra_test.go` — covers concurrent completion, config-failure fallback, and the injectable per-call timeout.

### Commands run
- Baseline: `bash .tigerteam/scripts/run-tests.sh`
  - `cmd:  bash scripts/test-all.sh`
  - `exit: 0`
  - `✔ Test run with 62 tests in 1 suite passed after 0.008 seconds.`
  - `ok  quotamon/internal/providers/deepinfra  1.195s`
  - `### exit 0`
- Formatting/audit: `gofmt -w core/internal/providers/deepinfra/sources.go core/internal/providers/deepinfra/deepinfra_test.go` and `git diff --check -- core/internal/providers/deepinfra/sources.go core/internal/providers/deepinfra/deepinfra_test.go` — clean.
- Final: `bash .tigerteam/scripts/run-tests.sh`
  - `cmd:  bash scripts/test-all.sh`
  - `exit: 0`
  - `✔ Test run with 62 tests in 1 suite passed after 0.007 seconds.`
  - `ok  quotamon/internal/providers/deepinfra  1.398s`
  - `### exit 0`

### Documentation
- Updated the `Fetch` doc comment to record that parallel calls protect the caller's refresh budget.
- Documented the exported `CallTimeout` field and its 8-second default.

### Commit
- `1cd793f` on `tigerteam/T-0023` (`[T-0023] Parallelize DeepInfra payment calls`).

### Review notes
- Authentication-error precedence is explicit when both calls fail; a successful usage call always remains available when config fails, as required.
- The injected 100 ms test still expects the specified fixed `>8 s` user-facing wording, since production's zero-value default is 8 seconds.
- No out-of-scope issues noticed; only the two scoped source files changed.

> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | codex-1 | gpt-5.6-sol | 2m 58s | 413.93k (376.32k) | 6.84k | — | — | review |

total: 1 attempt · 2m 58s · 413.93k in (376.32k cached) / 6.84k out · $0.00 · 1 without cost (accepted 2026-08-29T21:42:53Z)
