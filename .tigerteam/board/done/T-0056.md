---
id: T-0056
title: Omarchy: watchdog for a stuck quotamon fetch
priority: P2
complexity: C2
depends_on: []
scope: [omarchy/Panel.qml, omarchy/README.md]
---

## Goal
codex F12 (see `.tigerteam/reviews/2026-08-31-codex.md`): nothing bounds a
fetch; a hung child latches `refreshing` forever and disables Refresh.

## Design — implement exactly this
- A watchdog Timer armed when a fetch starts (30 s): on expiry, if
  `fetchProc.running`, call `fetchProc.signal(SIGTERM)`-equivalent (Quickshell
  Process API — use what exists: `running = false` forces kill if no signal
  API; document which), set `lastError = "quotamon timed out after 30s"`,
  clear `refreshing`, KEEP the previous snapshot. Disarm on normal onExited.
- The existing onExited path must handle the killed process (non-zero exit)
  without double-reporting (watchdog message wins).
- README: one line documenting the 30 s bound.

## Acceptance criteria
- [ ] `bash .tigerteam/scripts/run-tests.sh` exits 0 (node model tests unaffected)
- [ ] `grep -n "watchdog\|30000" omarchy/Panel.qml` shows the timer with a doc comment
- [ ] No files changed outside scope

## Out of scope
Model.js, BarWidget, multi-monitor behaviour, new settings.

## Worker report

### Summary

Implemented the watchdog for a stuck `quotamon` fetch in the Omarchy panel,
fixing review finding F12: previously nothing bounded a fetch, so a child that
hung forever left `refreshing=true` and permanently disabled Refresh.

### Files changed
- `omarchy/Panel.qml` — added a single-shot `watchdog` Timer (interval 30000)
  armed via `watchdog.restart()` whenever `refreshNow` starts a fetch; on expiry
  it hard-kills the child (`fetchProc.running = false` — Quickshell Process offers
  no SIGTERM method, so `running = false` is the force-kill, which is the same
  mechanism `refreshNow` uses to start the process), clears `refreshing`, sets
  `lastError = "quotamon timed out after 30s"`, and keeps the previous snapshot.
  The `onExited` handler now disarms the watchdog and gated on a new `timedOut`
  flag so the kill's non-zero exit does not overwrite the watchdog's message
  (no double-reporting).
- `omarchy/README.md` — documented the 30 s bound in one sentence.

### Commands run
- `bash .tigerteam/scripts/run-tests.sh` → exit 0. Result lines:
  `### node (omarchy model)` / `omarchy model tests passed`; Swift suite 83 tests
  passed; all Go package tests ok; `### exit 0`. Node model tests unaffected.

### Doc changes
- `omarchy/README.md`: one line documenting that a fetch hanging longer than 30 s
  is hard-killed and reported as a timeout while the last good snapshot is kept.

### Acceptance criteria verification
- [x] `run-tests.sh` exits 0 — yes (`### exit 0`)
- [x] `grep -n "watchdog\|30000" omarchy/Panel.qml` shows the timer with a doc
      comment (lines 93–94, 140, 142, 165–166)
- [x] No files changed outside scope (`git status` shows only Panel.qml + README.md)

### Commit
- `f0a058d` on branch `tigerteam/T-0056`

### Notes for the reviewer
- Chose `running = false` as the kill mechanism per the design's fallback since
  Quickshell's Process element exposes no signal/SIGTERM method; documented this
  in a comment at the Timer and in the README. The existing `nowMs` timer already
  uses interval 30000, so `grep 30000` matches both (expected).

> handoff: review
> INCOMPLETE attempt by ds-2 at 2026-08-31T15:08:28Z (engine exit 0, no handoff) — see /Users/d/b/quota_monitor/.tigerteam/logs/workers/ds-2.log

> claimed_by: ds-2 (pid 83690) at 2026-08-31T15:08:28Z
> handoff: review

## Accounting

| attempt | worker | model | wall | in (cached) | out | cost | source | route |
|---|---|---|---|---|---|---|---|---|
| 1 | ds-2 | deepseek-ai/DeepSeek-V4-Flash-0731 | 5m 23s | 289.13k (261.89k) | 7.39k | — | — | todo |
| 1 | ds-2 | deepseek-ai/DeepSeek-V4-Flash-0731 | 52s | 143.45k (136.45k) | 2.82k | — | — | review |

total: 2 attempts · 6m 15s · 432.58k in (398.34k cached) / 10.21k out · $0.00 · 2 without cost (accepted 2026-08-31T15:11:22Z)
